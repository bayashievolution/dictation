# dictation — 作業のときに読むこと

Chrome のサイドパネル拡張（MV3）と、同じコードを `serve.js` で配るブラウザ版。
やっさん（`bayashievolution`）が一人で使い、講義や会議の文字起こしに実運用している。

## セッションの最初にすること

**前回の更新から日が空いていたら、まずアップデート手順を案内する。**

やっさんはターミナルの常用者ではなく、久しぶりに触ると
「拡張をどう更新するんだったか」を忘れる。本人からの依頼:

> 話しかける時間が数日空いたら最初に アプデの方法を簡単に提示するようにしてほしい

判断のしかた（推測しないで、実際に測る）:

```bash
git log -1 --format=%cd --date=short    # 最後のコミット日
```

これと今日の日付が **2日以上離れていたら**、本題に入る前にひと声かける。

> 前回の更新から N 日空いています。拡張のアップデート方法を確認しておきますか？

「はい」なら `UPDATE.md` の要点（ブランチの切り替え →
`chrome://extensions` の ↻）を**その場に短く書く**。ファイルを読ませに行かせない。
「いらない」ならそのまま本題へ。**一度案内したら、そのセッションでは繰り返さない。**

同じセッションが何日も続くこともある。会話が途切れていたと分かるときは、
セッションの途中でも同じ判断をしてよい。

## 開発ブランチ

作業は **`claude/dictation-app-status-ra7l70`** で進める。`main` には入れない。
やっさんの手元も同じブランチを見ているので、
**「`main` を pull しても新しくならない」**のが久しぶりに触るときの一番の落とし穴。

## 版番号は3か所そろえる

更新のたびに、この3つを必ず同じ値にする。

| 場所 | 何のため |
|---|---|
| `manifest.json` の `version` | 拡張のカードに出る。更新できたかの確認に使う |
| `index.html` の `?v=` （全 script タグ） | 古い JS がキャッシュから読まれるのを防ぐ |
| コミットメッセージの見出し | 履歴から追えるように |

v0.20.0 のとき `index.html` だけ `?v=0.17.4` のまま取り残されていた前科がある。
`t-wire21.js` が manifest との一致を検査するので、**テストを通せば気づける**。

## テスト

ブラウザとマイクを CI で動かせないので、純関数を切り出して `node` の `vm` で
実物のまま駆動している。scratchpad に `t-*.js` として置く（リポジトリには入れない）。

**新しく直したときは、必ず変更前のコードに当てて落ちることを確認する。**
これで実際に2回、自分のミスが見つかっている
（ブロックリストの登録漏れ、区切り判定のテストの穴）。
テストが本当にその失敗を捕まえるか確かめないと、通っていること自体に意味がない。

## 保存されるキーは軽々しく変えない

`localStorage` と IndexedDB に実データが入っている。名前を変えると
**やっさんの手元の記録が読めなくなる**。表記だけ直したいときは、
内部名を据え置いて、なぜ食い違っているかをコメントに残す
（例: 画面は「話者判別」だが、キーは `session.repass` のまま）。

## 書きかたの約束

- 画面に出る文言は、**できないことをできると言わない**。
  消えたなら消えたと、ずれるならずれると書く
- コメントには「何をしたか」ではなく **「なぜそうしたか」「なぜ他の案を採らなかったか」** を書く
- 実機のログや実測で分かったことは、推測と区別して書く
