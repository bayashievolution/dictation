# dictation — PROJECT_DESIGN.md

講義・会議中の音声をリアルタイム文字起こし＆AI段落整形して、常時参照できるデスクトップアプリ。

---

## 📌 次セッションのカルディ２へ（2026-08-30 全体調査・3リポジトリ横断）

**このセクションを最優先で読む。** 2026-06-25 を最後に約 2 ヶ月止まっていたので、再開時に迷わないよう
`dictation` / `dictation-beta` / `dictation-overlay` の 3 リポジトリを突き合わせた調査結果を残す。
このあと同じセッションで **v0.14.0〜v0.14.2（Notion アップロード機能）** を実装した。
実装の経緯は試行錯誤ログの「2026-08-30 v0.14.x」を読むこと。

**久しぶりに触るときは、下の「開発・更新の手順」を先に読むこと。** WSL のディレクトリ、
Chrome 拡張の更新方法、HTML 版の起動、そして CRLF 事故の対処をまとめてある。

### 3 リポジトリの最新状態（2026-08-30 時点）

| リポジトリ | 役割 | 最新バージョン | 最終コミット | 技術 |
|---|---|---|---|---|
| `dictation` | 本番・安定版 | manifest `0.13.31` | `477efb1` 2026-06-25 WIP | Chrome拡張 + HTML版（Vanilla JS）|
| `dictation-beta` | β開発ライン（普段使い）| `0.13.31` | `875d0aa` 2026-04-27 | 同上 |
| `dictation-overlay` | OS レベル透過字幕オーバーレイ | `0.3.24` | `322cb66` 2026-04-27 | Rust + Tauri 2.0 |

いずれも `main` ブランチ 1 本のみ。

### 最重要：`dictation` と `dictation-beta` の作業ツリーが完全に同一

`diff -rq` で全ファイルを比較した結果、**差分は beta 側の `.claude/settings.local.json` 1 個だけ**。
app.js / captions.js / PROJECT_DESIGN.md まで含めてバイト単位で一致している。

原因は `477efb1`（2026-06-25 の WIP コミット）。β v0.13.31 のツリーを本番へ丸ごとコピーして一括コミットし、
メッセージ自身が「詳細分割は dictation カルディ２に委ねる」と書いたまま止まっている。

結果として **本番リポジトリの manifest.json が β を名乗ったまま**になっている：

| キー | 現在（`477efb1`）| 本来の安定版（`db2d5a1` v0.11.0）|
|---|---|---|
| `name` | ばっさんディクテーション **(β)** | ばっさんディクテーション |
| `version` | `0.13.31` | `0.11.0` |
| `description` | 【β版】…安定版と並行インストール可能 | （β表記なし）|
| `permissions` | sidePanel, storage, **nativeMessaging** | sidePanel, storage |

→ **このままでは本番として配布できない。** 配布を再開する前に必ずここを直す。
なお `index.html` の `<title>` と `.app-name` は β 表記なしなので、直す対象は manifest.json だけ。

### 配布形態は 3 つ（同じ index.html / app.js を 3 通りの入れ物で動かす）

| 形態 | 入口ファイル | 状態 |
|---|---|---|
| **Chrome拡張版** | `manifest.json`（MV3・サイドパネル）+ `background.js` | 現行主力。`chrome://extensions` から未パッケージ読み込み |
| **HTML版** | `serve.js`（Node 自前 HTTP・`127.0.0.1:8765`）+ `start.bat` | 現役。`file://` だとマイク許可が毎回聞かれるため localhost 配信にしてある |
| **Electron版** | `main.js` / `preload.js` / `package.json`（electron ^32）| **事実上デッド**。更新履歴 v0.4「Chrome前提Webアプリに方針転換」以降ノータッチ。トレイ・最前面・`Ctrl+Shift+D` は実装されたまま残っている |

- コード側の環境分岐は `app.js:1344` の `location.protocol === 'chrome-extension:'`（オンボーディング手順の出し分け）くらいしかない。
- **Native Messaging（overlay 連携）は `chrome.runtime.connectNative` 依存 = Chrome拡張版でしか動かない**
  （`captions.js:1330` `NATIVE_HOST = 'com.bayashi.dictation_overlay'`）。**HTML版では字幕オーバーレイは使えない。**

### これまでの更新のやり方（履歴から読み取れる運用）

- **1 コミット = 1 バージョン**。メッセージは `v0.13.31: 変更内容`（日本語）。overlay だけ `feat(v0.3.24): …` / `fix(…)` / `hotfix(…)` の Conventional Commits 風。
- **β で刻んで本番へ統合**。実例が `db2d5a1 v0.11.0: β(v0.10.x系)の全改修を本番に統合`。今回の `477efb1` も同じ「統合」を狙って途中で止まった形。
- `manifest.json` の version 更新漏れの前科あり（`ed6f58b v0.9.35: manifest versionを実装に合わせて更新`）。**コード側のバージョンを上げたら manifest も同時に。**
- ドキュメントは PROJECT_DESIGN.md に「試行錯誤ログ」＋「次セッションへの引継ぎ」を書く。overlay は加えて HANDOFF.md / NATIVE_MESSAGING_SPEC.md。
- `CLAUDE.md` はリポジトリ外（`~/.claude/CLAUDE.md`）にあるため、**リモートセッション（Claude Code on the web）からは見えない**。ルールが必要なときはこの md 内の「守るべきルール」を参照する。

### 積み残しの宿題（着手順は未定・やっさんと相談して決める）

1. **本番 manifest の β 表記剥がし**（上記）。配布を再開するなら前提条件
2. **`477efb1` WIP 巨大コミットの分割**（app.js +1611 / captions.js +1401 / captions-bootstrap.js 新規 / PROJECT_DESIGN +873）。本人が次セッションに委ねると明記している
3. **AI字幕整形が非表示のまま**。`captions.html` の `.cap-osd-section` に `hidden` を付与（beta `5002bd5`）。理由は「AI整形すると途中でエラーが出て消える／`→` の判定が正しい所とそうでない所がある」。機能本体（`osdAi` 設定・`formatOsdWithAi`・gemini.js のプロンプト）は残置してあるので、バグ取りすれば復活できる
4. **overlay v0.3.24 の新機能 3 つを拡張側が送っていない**。overlay の `src/main.js` は `streamMode`（YouTube 風行スクロール）/ `writingMode`（縦書き）/ `transition` を受け取れるが、`captions.js` の `buildOverlaySettings()` にこの 3 キーが無い。**overlay 側だけ実装済みで、拡張側から有効化する手段が存在しない**状態。機能追加をするならここが一番コスパが良い
5. `displayMode` の `flow` / `stream` は「あとで使うかも」で残置（UI・機能とも保持）— 前回からの継続
6. PROJECT_DESIGN.md 自体の整理（古い情報と新しい情報が混在）— **やっさんと相談してから着手**。前回からの継続の保留事項

### 作業環境についての注意（Claude Code on the web の場合）

- リモートセッションで push できるのは **`dictation` だけ**。`dictation-beta` / `dictation-overlay` は public なので読み取りクローンはできるが push 不可（必要なら `add_repo` で attach し直す）。
- `dictation` と `dictation-beta` の PROJECT_DESIGN.md は同一ファイル。**beta 側で作業する時はこのセクションを beta にもコピーすること**（現状 beta には入っていない）。

---

## 📌 前回の引継ぎ（2026-04-26 時点・最終 v0.13.31）

> 下の「直近の重要な学び」「守るべきルール」「役割分担」「やっさんからの大事な指針」は**現在も有効**。
> 「現状サマリ」「今日の作業の軌跡」は 2026-04-26 時点のスナップショットとして読むこと。

**コードを編集する前に試行錯誤ログ全体に目を通すこと**（このルールは今も有効）。

### 現状サマリ

- **dictation-beta v0.13.31**：真の「改行」方式（stop しない interim slice）+ 無音 stop（短い発話の取りこぼし防止）を実装。**ブロック間の言葉抜けゼロ**を達成（v0.13.30 の自滅事故からのやり直し）
- **dictation-overlay v0.3.4** との Native Messaging 連携も動作中（OS レベル透過字幕オーバーレイ）
- **主要設定**：
  - 「Web Speech チャンク間隔（秒）」セレクト（OFF / 6（既定）/ 8 / 10）：時間ベース強制 commit、stop() を呼ぶ。**残置（既存挙動を壊さないため）**
  - 「Web Speech 改行文字数（10〜100）」number-stepper（既定 30）：interim を N 字単位で改行＝新段落として字幕に流す。**stop() を呼ばない＝言葉抜けゼロ**
  - 「Web Speech 無音 stop（秒、0=OFF）」number-stepper（既定 3）：interim の中身が N 秒変化しない＝本当に喋りが止まった、と判定して stop()。短い発話を 6 秒待たせず字幕に流す。v0.13.30 の誤発火（onresult タイミング判定）は中身比較で回避

### 今日（2026-04-26）の作業の軌跡

| 範囲 | 内容 |
|---|---|
| v0.13.16〜v0.13.19 | 望む動作達成（Web Speech final 毎新段落 + appendRawChunk 内 snapshot/persist）|
| 整理ターン | 認識ズレ・他責構造を md に記録、PROJECT_DESIGN.md / CLAUDE.md 整理 |
| v0.13.20 | 不要コード撤去 → ミス、即座に v0.13.21 で revert |
| v0.13.22〜 | Web Speech チャンク間隔 UI 表示（既定 6）|
| v0.13.23〜v0.13.24 | interim 関連 UI と機能本体を段階的に撤去 |
| v0.13.25〜v0.13.27 | UI 文言整理 |
| v0.13.28 | 録音停止中に無音ダイアログ出るバグ修正 |
| v0.13.29 | チャンク間隔セレクトの空欄表示バグ（localStorage マイグレーション漏れ）修正 |
| v0.13.30 | **自滅事故**：「改行」をやっさんが言ったのに私が「final 化（recognition.stop）」と単語マッチで誤翻訳して実装。**同 commit を git revert で打ち消し**（v0.13.31）。事故の本質は「やっさんの問題提起の文脈を切り捨て、自分の実装都合で目的を歪めた」こと。試行錯誤ログ「v0.13.30 自滅事故」セクション参照 |
| v0.13.31 | **真の「改行」方式**を実装。recognition.stop() を呼ばず、interim を N 字単位にカットして appendRawChunk で新段落として transcript に流す方式。stop しない＝Web Speech は走り続ける＝**ブロック間の言葉抜けゼロ**。number input 10〜100、既定 30 |

### 直近の重要な学び（試行錯誤ログ参照、決して破ってはいけない）

1. **「やっさん明言」を引用するのも他責** — 「やっさんが指示してくれたから動いた」は、私が先に気づくべきだった責任を外部化している。やっさんの仕事を肩代わりさせている
2. **「やっさんとの認識ズレ」≠「カルディ２の中での分裂」** — 同一プロジェクト内で過去の自分（v0.13.9 を書いた私 vs v0.13.12 を書いた私）が別人状態になっている。外部化せず自分の中の問題として捉える
3. **「6 秒」は話者リズムで変えるパラメータ**（2回目の説明）— やっさんから 2 回同じ説明を受けた = ルール11違反の証拠
4. **選択肢を絞ったら localStorage マイグレーションも同時に**（v0.13.26→v0.13.29 の漏れ）
5. **変更で問題が出たら undo（git で戻す）。上書きで再現しようとしない**（v0.13.6→7→8 の Gemini プロンプト書き換え連鎖、v0.13.20 撤去ミスが典型）

### 守るべきルール（CLAUDE.md より、特に重要なもの）

- **ルール8**：変更で問題が出たら undo。`git diff` / `git checkout -- <file>` / Edit ツールで戻す
- **ルール9**：push 前に `node --check <file.js>` で構文検証
- **ルール10**：md はカルディ２のもの、やっさんに確認不要で自分で書く
- **ルール11**：反省会の内容は md に書く。書かないと次の私が同じ事故を繰り返す
- **ルール12**：他責にしない。能動態で書く（「思い込んでいた」→「私が確認しなかった」）

### 役割分担

| | 担当 |
|---|---|
| **やっさん** | 動作で判定（◯/×、こうしたい）|
| **カルディ２（私）** | コードの整合性を保つ（読み手のないコード・UI を見つけて整理する）|

私がコード整合性をサボると、やっさんが動作の違和感として気づいて指摘してくれるが、それは私の仕事の肩代わり。

### 保留中・後で考えること

- `displayMode='flow' / 'stream'`（v0.13.12 / v0.13.15 自動行スクロール）は「あとで使うかも」と判断して UI も機能も保持。captions.html の「字幕表示モード」セレクトに block / flow の 2 択あり
- PROJECT_DESIGN.md 自体の整理（古い情報と新しい情報が混在しているが、やっさんと相談してから着手）

### やっさんからの大事な指針（再確認）

- **「動作達成優先、コード整理は後」** — まず望む動作、その後で「使わないなら消す / ミスの元なら消す / あとで使うかもなら非表示で残す」
- **md はカルディ２のもの** — 確認不要で自分で書く
- **「N 秒」は話者リズム依存** — 既定 6 は岡田斗司夫テストの値、絶対値ではない。実用範囲は 6〜10 秒
- **他責にしない** — 受動態で逃げない、能動態で自分の責任として書く

---

## 開発・更新の手順（久しぶりに触るときは、まずここを読む）

2026-08-30、約2ヶ月ぶりに触った際に「WSL のディレクトリはどこか」「Chrome 拡張はどう
更新するのか」を思い出せず、さらに CRLF 事故（後述）を踏んだ。同じことを繰り返さないよう、
実際にやった手順をそのまま残す。

### ディレクトリ

| | WSL のパス | Windows から見たパス |
|---|---|---|
| 安定版 | `~/dictation`（= `/home/bayashi/dictation`）| `\\wsl.localhost\Ubuntu\home\bayashi\dictation` |
| β | `~/dictation-beta` | `\\wsl.localhost\Ubuntu\home\bayashi\dictation-beta` |
| オーバーレイ | `~/dictation-overlay` | `\\wsl.localhost\Ubuntu\home\bayashi\dictation-overlay` |

`~` は WSL のホーム。`/mnt/c/Users/ばやし` にいても `cd ~/dictation` で移動できる（Windows 側ではない）。
迷ったら `pwd && git remote -v && git log --oneline -1` で今どこにいるか確認する。

### Chrome 拡張を更新する

```bash
cd ~/dictation
git status --short   # ← 先に見る。未コミットの変更が無いか確認
git pull
```

そのあと Chrome で：

1. `chrome://extensions/` を開く（デベロッパーモードは ON のまま）
2. 拡張のカードの **↻（更新）ボタン**を押す
3. サイドパネルを開いていたら、一度閉じて開き直す

**manifest.json の権限（`permissions` / `host_permissions`）を変えたときは ↻ では反映されない
ことがある。** そのときはカードを「削除」→「パッケージ化されていない拡張機能を読み込む」で
フォルダを入れ直す。

**どのカードを押すか間違えやすい。** 安定版とβを並行インストールしていると、
安定版の manifest が「ばっさんディクテーション (β)」を名乗ったままなので**名前が同じに見える**。
カードの「詳細」を開いてフォルダのパスを確認してから操作すること。
（この名前の問題自体は宿題リストに残してある）

### HTML 版を起動する

`start.bat` をダブルクリック。中で `serve.js` を起動して `http://localhost:8765/` を開く。
`file://` だとマイク許可が毎回聞かれるので、localhost 配信にしてある。
**Notion 連携は HTML 版では動かない**（CORS。拡張版のみ）。

### まだマージしていないブランチで試す

```bash
git fetch origin
git checkout <ブランチ名>
git log --oneline -1     # 意図したコミットに居るか確認
```

戻すときは `git checkout main`。

---

### トラブル：`git diff` が巨大な差分を出す（CRLF 事故）

**症状**：`git checkout` が
「Your local changes to the following files would be overwritten by checkout」で止まる。
`git diff --stat` を見ると1万行以上の差分があり、**insertions と deletions の数が完全に同じ**。

**正体**：中身は1行も変わっていない。Windows 側のツールがファイルを CRLF で保存したことによる、
全行の改行コードの差分。

**確認方法**：

```bash
file app.js                              # → "with CRLF line terminators" が出るか
git diff --stat --ignore-cr-at-eol       # → 何も出なければ、差分は改行コードだけ
```

**`--ignore-cr-at-eol` で何も出なかった場合のみ**、捨てて良い：

```bash
git checkout -- .
```

**何かファイル名が出た場合は本物の変更が混ざっている。** 絶対に捨てず、一時ブランチに退避する：

```bash
git switch -c wip/$(date +%Y%m%d)-local && git add -A && git commit -m "WIP: 退避"
```

**再発防止**：2026-08-30 に `.gitattributes`（`* text=auto eol=lf`）を追加した。
以後は Windows 側から CRLF で保存されても git の中では LF に正規化されるので、この差分は出ない。

**やってはいけないこと**：確認前に `git reset --hard` や `git checkout -- .` を打つこと。
本物の変更が混ざっていた場合、復元できない。**必ず `--ignore-cr-at-eol` で確かめてから**。

---

## 目的

- 講義/会議中に「今なんて言った？」「どんな流れ？」に即応
- Word ディクテーションの壁文字問題（句読点なし・言い直し混入）を Gemini API で整形して解消
- Notion AIミーティングノート（月¥3000）の代替をほぼ無料で実現

## ターゲット環境

- Windows 11（ノートPC1枚運用、講義中）
- ブラウザ：Chrome（Web Speech API用）※初期バージョン
- デスクトップ：Electron（後期バージョン）

## 技術スタック

| レイヤー | 採用 | 理由 |
|---|---|---|
| 文字起こし | Web Speech API | 無料、ブラウザ組込、日本語対応 |
| AI整形 | Gemini 2.5 Flash | 無料枠が潤沢、高速 |
| デスクトップ化 | Electron | タスクトレイ/最前面/ホットキー対応 |
| UI | Vanilla JS + HTML/CSS | 軽量、依存なし |
| セッション保存 | localStorage (初期) → JSON file (Electron期) | クラッシュ時レジューム |

## 段階実装プラン

各段階で動作確認できる単位に分解。動かない状態で次の変更を重ねない。

1. **Step1**: Web Speech API生出力を表示（Chromeで動作確認）
2. **Step2**: Gemini API連携＋無音検出で段落整形
3. **Step3**: Electron化＋最前面/半透明/タスクトレイ/ホットキー
4. **Step4**: セッション自動保存＋クラッシュ時レジューム
5. **Step5**: 無音継続で停止確認ダイアログ
6. **Step6**: コピペ一発ボタン＋Markdownエクスポート

## DOM構造

```
#app
├── #titlebar          ... アプリ名・最前面/半透明/最小化/閉じるボタン
├── #controls           ... 録音開始/停止、コピー、エクスポート
├── #status             ... 録音中/停止中/整形中インジケータ
├── #transcript         ... メインビュー（スクロール）
│   ├── .paragraph     ... 整形済み段落（複数）
│   └── #interim       ... 未確定バッファ（薄文字、最下部）
└── #dialog             ... 無音停止確認用モーダル
```

## 状態モデル

### アプリ状態 `appState`

| 変数名 | 取り得る値 | 説明 |
|---|---|---|
| `recording` | `idle` / `listening` / `paused` / `stopped` | 録音状態 |
| `alwaysOnTop` | `true` / `false` | 最前面表示トグル |
| `transparent` | `true` / `false` | 半透明トグル |
| `silenceTimer` | `null` / timerId | 無音検出タイマー |
| `lastSpeechAt` | `Date` / `null` | 最後に音声が入った時刻 |

### 状態遷移

```
idle → (録音開始) → listening
listening → (無音60秒) → 停止確認ダイアログ表示 → paused or listening
listening → (停止ボタン) → stopped
stopped → (録音開始) → listening
```

### 転写データ構造 `transcript`

```js
{
  sessionId: string,
  startedAt: ISO datetime,
  paragraphs: [
    { id, rawText, refinedText, heading?, finalizedAt }
  ],
  interim: string  // 未確定バッファ
}
```

## 整形ロジック（Gemini連携）

### トリガー

- **無音検出**：Web Speech API の `result` で確定したテキストが入り、その後N秒（初期値: 3秒）無音が続いたら、そのチャンクを Gemini に送る
- **強制整形**：手動ボタンで残りバッファを即整形

### プロンプト設計

```
あなたは話し言葉を読みやすい文章に整える編集者です。
以下の直前の文脈と、新しい発話チャンクを渡します。
新しいチャンクを以下のルールで整形してください：
- 言い直し・フィラー（えー、あー、まぁ）を削除
- 句読点と改行を適切に補完
- 敬体/常体を直前段落に合わせる
- 話題が変わった場合、見出し（## 〜）を付ける
- 意味を変えない（推測で補足しない）

【直前の文脈】（2-3段落）
{context}

【新しい発話チャンク】
{newChunk}

【出力】整形後テキストのみ返す。見出しがあれば冒頭に ## 見出し\n で付ける。
```

### 整形結果の扱い

- 整形結果を `.paragraph` として追記
- 未整形バッファ `#interim` は整形完了と同時にクリア
- **整形中も録音は継続**（非同期処理）

## ボタン/操作の挙動

| 操作 | 挙動 |
|---|---|
| ▶ 録音開始 | Web Speech API起動、`recording=listening` |
| ⏸ 一時停止 | 録音停止（次再開で継続）、`recording=paused` |
| ⏹ 停止 | 録音停止＋残りバッファ強制整形、`recording=stopped` |
| 📋 コピー | 整形済み全文をクリップボードにコピー（見出し付きMarkdown） |
| 💾 エクスポート | `.md` ファイルとしてダウンロード |
| 📌 最前面 | `alwaysOnTop` トグル（Electron期以降） |
| 👻 半透明 | `transparent` トグル（Electron期以降） |
| Ctrl+Shift+D | ウィンドウ表示/非表示トグル（Electron期） |

## イベントハンドラの優先順位

1. `Escape` キー：録音中ならダイアログキャンセル、それ以外は無視
2. ウィンドウ閉じる：録音中なら「保存して閉じる？」ダイアログ
3. タスクトレイ最小化：録音継続、ウィンドウのみ非表示

## セッション保存

- **タイミング**：段落整形完了ごと、および30秒ごとに自動保存
- **保存先**：
  - Step1-2: `localStorage`（キー: `dictation:session:<sessionId>`）
  - Step3以降: `%APPDATA%/dictation/sessions/<sessionId>.json`
- **レジューム**：起動時に最新セッションを検出、「前回の続きを開く？」ダイアログ

## デザイン方針

### 世界観

- **実用ツール寄り**（ポップ要素控えめ）
- 講義中に目に優しいダークテーマ
- 読みやすさ最優先（Noto Sans JP、行間広め、フォントサイズ大きめ）

### 配色（初期）

- 背景: `#1a1a1f`（ダークグレー）
- 整形済み本文: `#e8e8eb`（オフホワイト）
- 未確定バッファ: `#6b6b73`（薄グレー、fade-in演出）
- 見出し: `#7dd3fc`（ライトシアン、読みやすい強調色）
- アクセント: `#34d399`（録音中インジケータ、グリーン）

### 半透明モード

- `background: rgba(26,26,31, 0.7)` + `backdrop-filter: blur(8px)`
- ウィンドウ全体を半透明に（Electron `transparent: true` + `vibrancy`）

## 試行錯誤ログ

> 実装中に試して却下した案・ハマった点を時系列で積む（堂々巡り防止）

### 2026-04-21 Electron での Web Speech API 制約

**症状**：Electron ウィンドウ内で Web Speech API が動かない（Chrome では動く）。

**原因**：Electron の Chromium には Google Speech API キーが同梱されていないため、`SpeechRecognition` が実際の認識結果を返さない。Chrome は Google の鍵で認証できる。

**試したが却下した案**：
- `GOOGLE_API_KEY` 環境変数を設定 → Gemini キーは Speech API とは別サービスで効かない
- Electron 側で権限ハンドラ追加 → 権限問題ではなく認証問題
- Gemini 音声入力に切替 → 5秒遅延が気になる、リアルタイム性低下

**採用した案（方針転換）**：
**Chrome前提の Web アプリ化**。Electron 化は一旦封印（ファイルは残置）。
メリット：サーバに上げれば他端末からも見られる／実装シンプル／Web Speech APIがそのまま使える。
失うもの：タスクトレイ常駐・常時最前面・半透明・グローバルホットキー（Chromeでは提供不能）。

### 2026-04-21 縮小ズーム(v<100%)の viewport 適合（**未解決・堂々巡り**）

**症状**：zoom 80〜95% で、以下いずれかが必ず発生
- case A: #app が viewport より右にはみ出し、「待機中」バッジやテキスト右端がクリップ
- case B: #app が viewport より小さく表示され、周囲に黒い「額縁」が出る

**試したアプローチ**（すべて本質的に同じ壁に当たった）：
1. `zoom: z` + `width: 10000/v %` → Chrome が layout の 111% を clip、右端切れ
2. `html, body { overflow: visible }` + 上記 → html スクロールバー発生
3. `transform: scale(z)` + `transform-origin: 50% 0` + no inverse → 額縁
4. `transform: scale(z)` + `transform-origin: top left` + 逆スケール → clip 同上
5. `transform: scale(z) translate(...)` + 明示中央寄せ → 額縁
6. `#app { width: 100vw; transform-origin: top left }` → 額縁

**根本的に両立不可能な理由**：
Chrome では `zoom` / `transform: scale` は**視覚のみ**スケールする。overflow clip は常に **layout 座標**で実行される。
- layout を逆スケールで拡大 → 祖先 (body/html) が overflow:hidden で clip → 視覚的に右端欠落
- layout をそのまま → 視覚が縮小されて viewport より小さい → 額縁

**実現可能な道**（どれを選ぶか要判断）：
- **A.** 縮小時に額縁を許容し、`body` の bg を **`--bg-elevated`** に揃えて「額縁のように見せない」
- **B.** **ズーム下限を 100%** にする（拡大のみ許可、縮小はブラウザの Ctrl+- に任せる）
- **C.** CSS custom property `--scale` で全要素の font-size / padding / gap を `calc(* var(--scale))` に書き換え。**大規模リファクタ（1–2時間）**だが「縮小でも viewport を自然に満たす」唯一の真の解

現時点では**未解決**。やっさんと相談して選択肢を確定してから着手する。

### 2026-04-21 Notion風の内側タブ構造を導入

「1セッション＝文字起こし／メモ／要約」の3面構成に変更。
- 文字起こし: Web Speech API のライブ結果
- メモ: 自由記述（contenteditable）
- 要約: セッション停止時に Gemini で自動生成＋手動「再生成」ボタン
- JSON保存／読み込み: セッション単位で `{transcript, memo, summary}` を往復可能

### 2026-08-30 v0.14.x Notion アップロード機能（タブ = ノート、ペイン = トグル）

やっさんの課題：「タブ機能はとても便利だが、日々使っているとどんどんタブが増える。
定期的に Notion にコピーして閉じているが、正直めんどくさい」。

#### 用語の整理（このプロジェクト最大の落とし穴）

このアプリの「タブ」は 2 種類あり、UI の文言が両者を混同していた。

| コード上 | UI の場所 | 実体 |
|---|---|---|
| セッション（`#tabs` / `state.sessions`）| 上部の外側タブバー | 増えていく方。タイトルが付き、閉じられる |
| ペイン（`#inner-tabs` / `PANE_META`）| その下の内側タブ | 文字起こし・メモ・要約・質問の 4 つ |

アプリ全体メニューの「**全タブを**プレーンテキストでコピー」は、実際には
`buildCombinedPlain()` が `state.settings.paneOrder` を回しているだけで、
**アクティブなセッション 1 個の中の 4 ペインを統合**していた。全セッションではない。
`btn-clear-all`（= `clearAllPanes`）も同様。この文言のせいで、やっさんと私の間で
「タブ」が指すものが最後まで一致しない危険があった。**先に用語を確認したのが正解だった。**

#### 決めたこと（やっさんとの Q&A で確定）

- **セッションタブ = Notion のノート 1 枚。ペイン = ノート内のトグル 1 つ**
- ペイン下のコピーボタンは、アプリ全体メニューのプレーンテキストコピーと重複 → **撤去**
- アップロードボタンは**アプリ全体メニューにのみ**設置し、
  「HTMLで保存」と同じ **クリック = このタブのみ / Shift+クリック・長押し = 全タブ** に統一
- コピー 2 つの挙動は変えない（ツールチップから「全タブを」を消すだけ）
- 保存先は**毎回聞くが、前回の保存先が選択済み**で出す（そのまま保存を押せば済む）
- 全タブ保存は**統合せず 1 タブ 1 ノート**。一部失敗したら**成功した分だけ閉じる**

#### Notion API の仕様調査（2025-09-03 版）

公式ドキュメント（developers.notion.com）はこの作業環境の egress プロキシで遮断されていたので、
**npm から公式 SDK `@notionhq/client` v5.26.0 を落として型定義を読む**方法で仕様を確定した。
これは今後も使える手。

- `Client.defaultNotionVersion = "2025-09-03"`
- この版から **データベース → データソース → ページ**の 3 階層になった。
  ページ作成の親は `database_id` ではなく **`data_source_id`**
- 保存先一覧は `POST /v1/search` の `filter: { property:"object", value:"data_source" }`
- **タイトル列の名前は DB ごとに違う**（名前 / Name / タイトル）。
  `GET /v1/data_sources/{id}` の `properties` から `type === 'title'` の列を引く必要がある
- 上限：`children` は 1 リクエスト 100 ブロック、`rich_text` 1 要素は 2000 文字

#### CORS の制約（HTML 版では動かない）

`api.notion.com` はブラウザに CORS ヘッダを返さない。Chrome 拡張のページは
`host_permissions` に載っているホストなら CORS の対象外になるので**拡張版でだけ動く**。
HTML 版（`serve.js` の localhost:8765）では fetch がブラウザに弾かれる。
→ `notionIsAvailable()`（`location.protocol === 'chrome-extension:'`）で分岐し、
HTML 版ではアップロードボタンを disabled にして理由を title に出す。
どうしても HTML 版でも使いたくなったら、`serve.js` に中継エンドポイントを足すことになる。

#### 実装で気をつけた点

- **タイトル列の名前は 1 回だけ引いて使い回す**。最初は 1 ノートごとに GET していたが、
  20 タブ一括だと 20 回無駄に叩くうえレート制限に近づく
- **429 / 5xx は指数バックオフで 3 回まで自動リトライ**（Notion は毎秒 3 リクエスト程度で制限）。
  `Retry-After` ヘッダがあればそれに従う
- **録音中のタブは一括保存の対象から外す**。保存直後に閉じられると録音が止まってしまう
- 閉じるときは `closeMultipleSessions(ids, { skipConfirm: true })`。
  「Notion に保存できました。閉じますか？」を出した直後に
  `closeSession` の「内容は削除されます」がもう一度出るのは邪魔なので抑止した
- **保存先ピッカーは Promise で結果を待つ**ので、Escape・背景クリック・× のどれでも
  必ず resolve するようにした。1 経路でも漏らすと UI が固まる

#### 検証方法（実機なしでどこまで確かめたか）

Chrome 拡張として実際に Notion に投げるところは、この環境では確かめられない。
代わりに **jsdom で `notion.js` を読み込んで、変換と分割を実際に動かして**確認した：

- メモの h1/h2・段落・チェックボックス（checked 含む）・ul/ol・引用・区切り線・太字が、
  対応する Notion ブロックになること
- 文字起こしの入れ子 div（`.paragraph > .p-body`）が段落として拾えること
- **空行 `<div><br></div>` が空段落として送られてしまうバグをこの検証で発見して修正**した。
  メモエディタは空行をこの形で持つので、そのままだと Notion 側が空段落だらけになっていた
- 4500 文字の段落が rich_text 2000/2000/500 に分割されること
- 250 ブロックのトグルが「作成時 100 + 追記 100 + 追記 50」に分割送信されること
- parent が `data_source_id`、タイトル列が DB 実物の列名になること

#### 実機検証（2026-08-30 完了）

やっさんの環境で通した。**接続テスト・実アップロードとも成功**（「完璧だー！」）。

- 接続テスト：`OK: 講義ノート自動作成 — 保存先に使えるデータベース 3 件`
- 実アップロード：ノートが作られ、ペインがトグルとして入ることを確認

既存のコネクト「講義ノート自動作成」をそのまま流用できた。新規作成は不要で、
権限に「コンテンツを挿入」があれば足りる。

#### Notion 側の用語が変わっていた（v0.14.3 / v0.14.4）

実機で試して初めて分かった。私が書いたヒント文が古い用語のままで、
やっさんがどこを触ればいいか分からない状態になっていた。

| 以前 | 現在 |
|---|---|
| インテグレーション | **コネクト**（一覧は「コネクション」）|
| 内部インテグレーションシークレット | **アクセストークン** |
| インテグレーション設定ページ | **開発者ツール → コネクション**タブ（作成は「+ 新規コネクト」）|

ただしコネクト詳細画面は「設定」タブの中にセクション見出し「インテグレーショントークン」、
その中のフィールド名が「アクセストークン」という**混在表記**。
アプリのヒント文は「コネクトを開く →『設定』タブ →『インテグレーショントークン』→
『アクセストークン』」と、実際にたどる順路そのままに書いてある。

**教訓**：外部サービスの UI 文言は、書いた時点の記憶で書くと古くなる。
今回は公式ドキュメントが作業環境から見えなかったこともあり、やっさんのスクリーンショットが
唯一の一次情報だった。**外部サービスの手順を書いたら、実機のスクショで裏を取る。**

---

### 2026-08-31 v0.18.0 チャンクを無音の位置で切る（v0.17.1／v0.17.2 の根治）

v0.17.1（捏造）と v0.17.2（断片の繋ぎ直し）は、どちらも**対症療法だと自分で書いていた**。
根本原因は「チャンクを12秒の時計で機械的に切っている」こと。ここを直す。

#### v0.17 までの切り方の何が悪かったか

```js
setInterval(() => recorder.stop(), 12000);   // 喋っている真っ最中でも問答無用
```

- 単語の途中で切れる → 次のチャンクが「はないので」から始まる → Gemini が頭を創作（v0.17.1）
- 切れた両側が別々の段落として残る（v0.17.2 で繋ぎ直す羽目になった）
- **さらに `stop → start` の再開に 40ms の空白があり、その間の音はどこにも記録されない。**
  発話の途中で切ると、その 40ms 分の音が消える（これは今まで誰も気づいていなかった）

#### 変更後

「最短12秒を過ぎたら、次の無音の切れ目で切る。20秒たったら諦めて切る」。
判断は `decideChunkCut()`（純関数・テスト済み）に切り出した。

```
0 ─────── 12秒 ─────────────── 20秒
|  絶対切らない | 無音になったら切る | 無音でなくても切る
```

無音の位置で切れば、40ms の空白も無音に落ちるので**音の欠落も同時に消える**。

#### しきい値を固定値にしなかった理由

やっさんは「小さい声を拾ってくれるから」Gemini モードを使っている。
`-40dB` のような固定値にすると、静かな部屋の小声が丸ごと「無音」に落ちて、
**発話の途中で切るという直したかった失敗に戻る**。
そこで暗騒音（ノイズフロア）を推定し、そこから何 dB 上かで判定する。

#### ノイズフロア推定でつぶした失敗（すべて jsdom のテストで検出した）

最初の実装は「常時 EMA 追従（下 0.25 / 上 0.002）、初期値 -60dB」。合成音でテストしたら3つ落ちた。

| 失敗 | 何が起きたか | 直し方 |
|---|---|---|
| **喋り続けるとフロアが持ち上がる** | -25dB で1分喋るとフロアが -28dB まで上昇し、**発話そのものが無音に見えた**。直したい失敗そのもの | フロアの学習を「静かだと判定できているときだけ」に限定。発話中は一切動かさない |
| **うるさい部屋で無音が見つからない** | 初期値 -60dB 固定だと、暗騒音 -45dB の部屋では上方向の追従が遅すぎて暗騒音がずっと「発話」に見える | 開始1秒間を実測にあて、その場の暗騒音から始める |
| **喋りながら録音開始すると全部無音に見える** | 初期実測が発話レベルを掴む。そのまま切ると Gemini に「きれいに切れた」と**嘘を伝える** | フロアより 10dB 上の音を一度も聞いていない間は無音判定を許さない（`heardSpeech`）。「静か」は相対的な概念で、大きい側を知らないうちは判定できない |

3つ目は特に、今回の一連の作業で学んだこと（**モデルに入力の性質を正直に伝える**）を
自分で破りかけていた。判定できないなら黙って最長秒数で切るほうが正しい。

#### 切り口を Gemini に正直に伝える（プロンプト側）

v0.17.1 で入れた「この音声は断片です」は**全チャンクに無条件で言っていた**。
無音位置で切るようになると大半のチャンクは文の切れ目で始まり切れ目で終わるので、
それを断片だと言い続けるのは今度はこちらの嘘になり、
**最後まで聞こえている文まで途中で止めさせかねない**。

`buildChunkEdgeRule(edges)` で切り口の実態に合わせて言うことを変える。

| 頭 | 尻 | モデルに言うこと |
|---|---|---|
| 無音 | 無音 | 前後とも切れ目。ひとまとまりのはず |
| 強制 | 無音 | 頭だけ警告（前を補うな）。終わりは最後まで聞こえている |
| 無音 | 強制 | 尻だけ警告（続きを作るな）。頭から聞こえている |
| 強制 | 強制 | 両方警告（＝ v0.17.1 と同じ） |

次チャンクの「頭」は前チャンクの「尻」と必ず一致する（同じ切り口の裏表）ので、
`onstop` の再スタート時に引き継ぐ。

#### フォールバック

`AudioContext` が作れない／suspended のまま／設定で OFF のときは、
`silenceDetector` が null になり**従来どおりの固定間隔**に落ちる。録音自体は止めない。
`heardSpeech` が立たない場合も同じく最長秒数での強制区切りになるだけ。
**最悪でも v0.17 と同じ挙動**で、それ以下にはならない。

#### 実機テストで出た副作用（v0.18.1 で修正）

実機で確認したところ、狙いどおり**文の途中で切れた段落は一つも出なくなり**、
3秒の沈黙がちょうど段落の切れ目になった。一方で末尾に
`（音声不明瞭・再試行可）` が必ず出るようになった。

原因は v0.18.0 が作り出した新しい状況。無音位置で区切るということは、
**最後の区切りのあとに「中身が沈黙だけ」のチャンクが必ず1本できる**ということ。
それを送ると Gemini は正しく空を返し、既存のコードはその空応答を
「要再試行」として画面に残していた（v0.17.0 で「Gemini が空でも Web Speech の
結果は捨てない」ために入れた分岐）。

消す条件は厳しくした（`shouldDropEmptyChunk`）。**間違えると小声を黙って
捨てることになる**ので、次の3つが揃ったときだけ消す:

- Gemini が空を返した
- Web Speech も何も拾っていない（別エンジンでも聞こえていない）
- 無音検出器がそのチャンク中に発話音量を一度も見ていない

検出器が無い構成では 3つ目が `undefined` になるので、従来どおり残す。

なお「沈黙だけと分かっているなら送らない」という手もあるが、採らなかった。
検出器の発話判定はフロア +10dB で、それを下回る小声は拾えない。
送信自体をやめると、その小声が**どこにも残らず消える**。
表示を消すだけなら Gemini が空だと確認済みなので、失うものが無い。

#### 無音のしきい値が短すぎた（v0.18.2）

v0.18.1 の実機テストで、末尾の「（音声不明瞭・再試行可）」は出なくなった一方、
新しい分断が出た。

```
…きちんと開業されているようです。しかし、最初冒頭     ← ここで切れた
の方に喋り始めた「今喋り続けています。…
```

「冒頭の方に」が真っ二つ。これは無音区切りが**効きすぎた**ケース。
`SILENCE_HOLD_MS = 350` は「文の切れ目」ではなく「言いよどみ」まで拾ってしまう。
日本語は考えながら喋ると 350ms を軽く超える間が空く。

チャンクの長さからも裏付けが取れる。このチャンクは沈黙2.6秒＋発話8秒程度で、
ちょうど最短の12秒に届いたあたり。**切ってよくなった瞬間、最初の言いよどみで
切っている**。

かといって一律に長くすると、切ってよい場所が見つからず強制区切りが増える
（＝ v0.17 に戻る）。そこで段階的にした。

| 経過 | 必要な無音の長さ | 狙い |
|---|---|---|
| 最短〜(最長-4秒) | 700ms | 文の切れ目だけを狙う |
| (最長-4秒)〜最長 | 350ms | 強制で切るよりはマシ、と妥協する |

しきい値の判断は検出器から `decideChunkCut()` に移した（検出器は「いま何 ms
静かが続いているか」を返すだけにした）。判断が1か所に集まり、テストできる。

診断ログにチャンクの長さも出すようにした。`長さ13.2秒 切り口 頭=無音 尻=無音`
のように出るので、強制区切りばかりになっていないかを実機ログで見分けられる。

#### ノイズフロア推定のデッドロック（v0.18.3・これが本命の不具合だった）

v0.18.2 の実機ログ:

```
録音開始 (Gemini) 最短=12秒 最長=20秒 (無音位置で区切る)
音声チャンク送信 321974B 長さ20.0秒 切り口 頭=無音 尻=強制
音声チャンク送信 322940B 長さ20.0秒 切り口 頭=強制 尻=強制
音声チャンク送信 112352B 長さ7.0秒  切り口 頭=強制 尻=強制
```

**3秒黙ったのに無音が一度も検出されず、全チャンクが最長20秒に張りついた。**
700ms が長すぎるという話ではない（3秒は 700ms を余裕で超える）。
無音区切りが最初から一度も機能していなかった。

原因はデッドロック:

1. 録音開始直後は AudioContext にまだサンプルが流れておらず、解析結果はゼロ = -99dB
2. それが「開始1秒の実測」に入り、**ノイズフロアが -99dB で確定**する
3. `quiet = db < -99+6 = -93dB` は実際の音（-60〜-20dB）では絶対に成立しない
4. フロアの学習は v0.18.0 で「静かなときだけ」に絞ってある
   → **-99 から一生回復できない**

4 は「喋り続けるとフロアが持ち上がる」のを防ぐために入れた対策だった。
それが逆方向（フロアが低すぎる）から抜け出せない作りを生んでいた。
**片方向にしか動けない推定を採ったのが設計の誤り。**

##### 作り直した推定

片方向の EMA をやめ、**直近の窓の最小値**をフロアにした。最小値なら両方向に
自己修正する。窓は 4 秒バケツ2本＝直近 4〜8 秒。

最小値だけだと、同じ音量で喋り続けたときにフロアが発話レベルに張りつく。
そこで**同じ窓の最大値**も持ち、最大と最小の開きが 10dB 未満なら
「大きい側と小さい側の区別がついていない」として判定を放棄する。
（v0.18.0 の `heardSpeech` を、一度きりの判定から窓ごとの判定に置き換えた形）

完全なゼロ（-95dB 未満）は「静かな部屋」ではなく「まだ音が流れていない」ので、
フロアの材料からも無音判定からも外す。これが 2 の直接の修正。

##### なぜテストで捕まえられなかったか

合成音のテストは**1ティック目から設定した音量を返していた**。実機の
「まだサンプルが来ていない」状態を再現していなかった。
ケース7として追加し、**v0.18.2 の実物に当てて実際に落ちることを確認した**
（「3秒黙れば切れ目を検出する」で false になる）。

##### 診断ログを足した

v0.18.2 のログには「尻=強制」としか出ず、検出器の中で何が起きているか
分からなかった。**そのチャンク中でいちばん長かった無音**を出すようにした。

```
長さ13.2秒 切り口 頭=無音 尻=無音 [床-62 天-28 判定可 最長無音1150ms ctx=running]
```

- 最長無音 3000ms なのに強制で切れている → 区切りの判断がおかしい
- 最長無音 0ms なのに黙っていた → 検出器がおかしい
- `ctx=suspended` → AudioContext が動いていない

#### 音量が平坦化されていて判定できなかった（v0.18.4）

v0.18.3 の診断ログ:

```
長さ20.0秒 切り口 頭=無音 尻=強制 [床-38 天-34 判定不能 最長無音0ms ctx=running]
```

**20秒の中に発話と3秒の沈黙が両方入っているのに、音量の差が 4dB しかない。**
生の音声なら 30〜40dB は開く。何かが音量を平坦化している。

`autoGainControl: false` は v0.13.6 から要求済み（VB-Cable で独り言が混入する
事故の対策）。なので Chrome の外側 ── Windows の音声拡張機能やドライバ、
マイク本体 ── で平坦化されていることになる。**こちらから止められない。**

診断そのものは正しく働いた。「判定不能」と申告して強制区切りに落ちており、
嘘の「きれいに切れた」は一度も出していない。設計どおりの負け方をしている。

##### 音量に依存しない第二の手段

Web Speech が既に並走している（v0.17.0 のリアルタイム表示）。
**音声認識器が「いま声が出ているか」を判断してくれる**ので、音量が平坦でも効く。
`onresult` で文字が来た時刻を記録し、そこからの経過を無音の長さとして使う。

優先順位を明確にした（`pickSilentMs`・純関数）。**2つを混ぜない**のが要点で、
`max()` などで混ぜると片方が壊れているときに「きれいに切れた」と嘘をつく。

| 条件 | 使う手段 |
|---|---|
| 音量で判定できる | 音量（合成音で検証済みの経路） |
| 音量が判定不能・Web Speech が動いている | Web Speech |
| どちらも無理 | 0 ＝ 最長秒数での強制区切り（v0.17 と同じ） |

##### 診断を増やした

- **マイクの実設定**を `getSettings()` で出す。要求した制約が本当に効いているかは
  これでしか分からない
- **チャンク全体**の音量の振れ幅。v0.18.3 は直近4〜8秒の窓しか出しておらず、
  そのチャンクにそもそも十分な差があったのかが分からなかった
- どの手段で切ったか（`採用=level` / `webspeech` / `none`）

##### 反省

v0.18.0〜v0.18.3 は「音量で無音を測る」という一つの前提の上に積み上げていた。
その前提は、**利用者の環境では最初から成り立っていなかった**。
実機のログを見るまで4回それに気づけなかった。
測れているかを先に確かめるべきだった。

#### 測る場所が間違っていた（v0.18.5・v0.18.4 の説明の訂正）

v0.18.4 で「Chrome の外側で音量が平坦化されている」と書いたが、**これは誤り**。
やっさんから利用状況を聞いて分かった:

> いま無音の自室で、パソコンの近くで、どちらかというと小声でしゃべっているよ
> あとはエアコンが効いているくらい

エアコンの音は**低域が主**で連続している。全帯域の RMS を測ると、その値は
低域に支配され、上に乗っている小声の差が埋もれる。外部が平坦化していたのではなく、
**エアコンの音を「音声の音量」として測っていた。**

Gemini と Web Speech は聞き取れているのに測定だけ差が出ない、という食い違いが
そのまま証拠だった。

##### なぜこれは会場が変わっても効く話なのか

会場で問題になる暗騒音は、空調・プロジェクタの送風・PC のファンなど、
たいてい低域が主。**全帯域で測っている限りどの会場でも同じ失敗をする。**
つまりこれは自室固有の問題ではなく、設計の誤りだった。

##### 直し方

人の声の帯域（300〜3400Hz・電話帯域）だけを見る。`getFloatFrequencyData()` で
そのビンだけを合計する。`smoothingTimeConstant` は 0 にする（既定 0.8 だと
平滑化されて短い切れ目が鈍る）。「音が流れているか」の確認だけは時間領域の
広帯域値を使う（帯域を絞った値はゼロ入力でも -99 とは限らないため）。

合成音のテストで、同じ音に対して:

| 測り方 | 沈黙と小声の差 |
|---|---|
| 全帯域（v0.18.4 まで） | **0.3dB** ← 判定不能 |
| 声の帯域（v0.18.5） | **15dB** ← 判定可 |

##### テストのモックも作り直した

v0.18.4 までのモックは全帯域一様の信号しか作れず、
「低域に埋もれて差が出ない」という実機の失敗を**構造的に再現できなかった**。
声の帯域と低域を別々に置けるようにして、ケース12（空調＋小声）と
ケース13（会場が変わって空調が30dB大きくなる）を追加した。

##### 会場ごとの想定

| 環境 | 効くか |
|---|---|
| 静かな部屋＋空調 | ◎ 声の帯域で分離できる（ケース12） |
| 会議室・空調が大きい | ◎ フロアは実測なので絶対値のしきい値より強い（ケース13） |
| 小声・ノートPCの内蔵マイク | ○ 相対判定なので原理的に対応。差が10dB取れなければ判定を放棄する |
| 反響のある講義室 | △ 残響で話し終わりがなだらかになり 700ms 取れないことがある → 強制区切りに落ちる |
| ハンドマイク／PA 経由 | △ PA のコンプレッサで平坦化されうる → Web Speech が受ける |
| 遠いマイク＋ざわついた会場 | △ Web Speech も弱くなる → 強制区切りに落ちる |

△ はいずれも**壊れるのではなく v0.17 と同じ動作に戻るだけ**。
判定できないときに「きれいに切れた」と主張しない設計にしてあるので、
最悪でも v0.17 相当で、それ以下にはならない。

#### v0.18.5 の実機結果と、残っていた2つ（v0.18.6）

```
マイク実設定 AGC=false ノイズ抑制=false エコー除去=false 48000Hz
長さ13.1秒 頭=無音 尻=無音 [声帯域 全体-78〜-47(幅31dB) 判定可 最長無音1350ms 採用=level]
長さ16.4秒 頭=無音 尻=無音 [声帯域 全体-80〜-48(幅33dB) 判定可 最長無音 950ms 採用=level]
長さ 6.9秒 頭=無音 尻=無音 [声帯域 全体-80〜-44(幅36dB) 判定可 最長無音1300ms 採用=level]
```

帯域を絞ったことで幅が 4dB → 31〜36dB になり、強制区切りがゼロになった。
`AGC=false` も実際に効いていた（v0.18.4 の「外部で平坦化」説は完全に否定された）。

残っていた不具合は2つ。

##### 1. 最後のチャンクだけ繋ぎ直しから漏れていた

```
18:34:35 録音停止
18:34:36 ミドル整形開始 2段落      ← まだ2つしか届いていない
18:34:36 音声チャンク送信 6.9秒    ← 最後のチャンクはこの後
```

停止時の最終整形が `flushPendingToGemini()` の後に走るが、**最後の音声チャンクは
`recorder.stop()` → `onstop` → `sendAudioChunkToGemini()` という非同期経路**で、
これは待たれていなかった。結果、最後の段落だけが `.short-refined` のまま孤立し、
文の途中で切れたまま残る。

`ensureTranscriptSettled()`（v0.17.3）は確認ダイアログを出す対話用なので流用できず、
黙って待つ `waitForAudioSettled()` を足した。`recorder.stop()` から `onstop` までの
隙間でカウンタが 0 に見える問題は `finalChunkPending` フラグで埋める。

##### 2. 950ms の言いよどみを文の切れ目と判定していた

`どのように / だったでしょうか` が真っ二つになった。
**v0.18.5 で測定がまともになって初めて、実際の間の長さが分かった。**

| 長さ | 何だったか |
|---|---|
| 1350ms | 文末（「〜黙ってみます。」） |
| 1300ms | 文末（「〜終わりたいと思います。」） |
| **950ms** | **文の途中の言いよどみ** |

700ms → 1000ms（妥協ラインは 350ms → 500ms）。
v0.18.2 で 350→700 にしたときは測定自体が壊れていたので、根拠のない数字だった。
今回は実測値の間に線を引いている。ただし**1回の録音から取った値**で、
話し方が変われば見直しが要る。外すほうに転んでも強制区切りに落ちるだけ。

### 2026-08-31 v0.18.8 Web Speech の遅れで最後の一文が二重に出た

v0.18.7 で要約の捏造は直った（123字の録音から話した内容だけの短い要約が出た）。
代わりに、最後の一文が二重に表示された。

```
はい、3秒間黙りました。どのようになっているでしょうか。はい、ではまた報告します。
                                        ← Gemini が正しく文字起こしした分
はい 3秒間 黙りました どのようになっているでしょうか はい ではまた 報告します
                     [Gemini は聞き取れず・Web Speech の結果です]  ← 同じ内容
```

#### 原因

```
18:48:30 チャンク3(16.0秒) 尻=無音   ← この音声に「はい、3秒間黙りました…」が入っている
18:48:32 録音停止
18:48:32 チャンク4( 2.3秒) 尻=強制   ← 残りのほぼ沈黙
18:48:35 Gemini リトライ 1/1 (empty) ← チャンク4 は空だった
```

**Web Speech の「確定」は遅れて届く。** チャンク3の音声に対応する文字が、
チャンク3を切った**後**に確定し、チャンク4の「未確定分」として付いた。
そこで v0.17.0 の「Gemini が空でも Web Speech の結果は捨てない」が働き、
すでに画面にある文がもう一度出た。

v0.17.0 で「チャンクの区切りで受け渡すので時間軸を突き合わせる必要がない」と
書いたが、**それは Web Speech が区切りまでに確定していることを前提にしていた。**
確定が区切りをまたぐ場合を考えていなかった。

#### 直し方

時刻で対応づけ直すことはできない（Web Speech は確定の元になった音声の時刻を
教えてくれない）。なので「**すでに画面にあるものは出さない**」で対処する。

Gemini と Web Speech は表記が違う（「はい、3秒間黙りました。」と
「はい 3秒間 黙りました」）ので完全一致では判定できない。空白・句読点・注記を
落としたうえで、2文字の並びの重なりが 70% 以上なら同じ内容とみなす。

**消してよいと分かるのは同じ内容が確かに残っているときだけ**なので、
取りこぼしにはならない。短すぎる文字列（8文字未満）は偶然一致しうるので
判定しない（取りこぼすより残すほうがマシ）。

#### この区切りの他の観察

```
チャンク1 20.0秒 尻=強制 最長無音454ms   ← 喋り続けていたので切れなかった
チャンク2 18.3秒 尻=無音 最長無音500ms   ← 最長の4秒前からの妥協ライン(500ms)で切れた
チャンク3 16.0秒 尻=無音 最長無音1300ms  ← 文末で切れた
```

v0.18.6 で入れた「最長が近づいたら妥協する」段階しきい値が、チャンク2で
実際に働いている。除湿機を除湿モードにして低音が増えた状態でも幅は33〜35dB
出ており、帯域を絞った効果が環境変化に耐えている。

---

### 2026-08-31 v0.18.9 明らかな切れ目を「最短より前だから」と捨てていた

v0.18.8 で二重表示は直った。実機ログ（19:00）にはっきりした失敗が残っていた。

```
19:00:22 チャンク1 20.0秒 尻=強制 [判定可 最長無音 653ms 採用=level]
19:00:42 チャンク2 20.0秒 尻=強制 [判定可 最長無音 547ms 採用=level]
19:01:02 チャンク3 20.0秒 尻=強制 [判定可 最長無音3800ms 採用=level]  ← これ
```

**3.8秒の沈黙を検出していたのに、そこで切らず20秒で強制区切りした。**
v0.18.3 で診断ログを足したときに「最長無音3000msなのに強制 → 区切りの判断が
おかしい」と書いた、まさにその形。

#### 原因

その沈黙は「3秒今から黙ってみます」の直後、チャンク開始から約6秒の時点で起きた。
**最短12秒より前なので使えないことになっていた。**

結果は文字起こしの末尾に出ていた。

```
…読み取り結果のプレーンテキストをコピーした結果と、ログの結果をコピーして送り
以上です。
```

「送り」で切れて、続きの「たいと思います」が失われている。20秒での強制区切りが
単語の途中に落ちたため。

#### 直し方

最短を設けたのは「短すぎるチャンクは文脈が無くて精度が落ちる」ため。だが
直前の文脈は `contextHint` で渡しているし、短チャンクはミドル整形でまとまる。
**明らかな切れ目で終わった短いチャンクは、文の途中で切れた長いチャンクより良い。**

```
0 ─── 3秒 ────────── 最短(12秒) ─────── (最長-4秒) ─── 最長(20秒)
  切らない │ 2秒以上の無音なら切る │ 1秒 │ 0.5秒 │ 強制
```

3秒の下限は、極端に短いチャンクを作らないための歯止め。

#### 整形が2回に割れていた

```
19:00:07 ミドル整形開始 3段落・224字   ← 3つ溜まって発火
19:00:09 ミドル整形開始 1段落・5字     ← 最後の1つが取り残された
```

チャンク3の応答が届いた時点で「3段落溜まった」条件が満たされ、停止処理が
最後のチャンクを待っている最中に先に発火していた。分かれると境界をまたいだ文を
繋ぎ直せないうえ、API 呼び出しも1回余計になる。

停止処理中（録音していない かつ 送信中のチャンクが残っている）は
`maybeConsolidateShortChunks` を発火させず、停止処理の最後で全部まとめて
整形するようにした。

#### しきい値についての正直な話

同じ話者でも、文末の間の長さは録音ごとにかなり違う。

| 録音 | 各チャンクの最長無音 |
|---|---|
| 18:34 | 1350 / 950 / 1307ms |
| 18:47 | 454 / 500 / 1300 / 945ms |
| 19:00 | 653 / 547 / 3800 / 705ms |

**文末（1300ms）と言いよどみ（950ms）は分けられるが、
文末（653ms）と言いよどみ（950ms）は逆転していて分けられない。**
無音の長さだけでは、原理的に完全な判定はできない。

だから設計の軸は「完璧に切る」ではなく「**外したときに壊れないこと**」に置いてある:

- 切り口が強制なら Gemini にそう伝える（続きを作らせない）
- 文の途中で切れた断片はミドル整形で繋ぎ直す
- どうにもならなければ最長秒数で切る（＝ v0.17 と同じ）

---

### 2026-08-31 v0.18.10 一瞬の物音を「発話」と取り違えていた

v0.18.9 の実機結果（20:36・炊飯器が動いている条件）は狙いどおりだった。

```
18.0秒 頭=無音 尻=無音 [幅33dB 判定可 最長無音1251ms 採用=level]
16.0秒 頭=無音 尻=無音 [幅31dB 判定可 最長無音1000ms 採用=level]
11.2秒 頭=無音 尻=無音 [幅31dB 判定可 最長無音2050ms 採用=level]  ← 最短12秒より短い
 1.0秒 頭=無音 尻=無音 [幅12dB 判定可 最長無音2101ms 採用=level]
```

**4本すべてが `頭=無音 尻=無音`、強制区切りゼロ。**
3本目が 11.2 秒で切れているのは、v0.18.9 で入れた「最短前でも明らかな切れ目なら
使う」が働いた証拠。炊飯器の音が加わっても幅は 31〜33dB を維持している。

残っていたのは末尾の「（音声不明瞭・再試行可）」。

#### 原因

4本目は 1.0 秒で、Gemini は空、Web Speech も何も拾っていない。
それでも消えなかったのは `hadSpeech` が **true** だったから。

`speechInChunk` は「フロア+10dB を超えるフレームが**1つでも**あれば true」に
していた。1フレーム = 50ms なので、**舌打ち・クリック音・前の発話の余韻**でも
成立する。実際このチャンクの幅は 12dB で、ぎりぎり超えていた。

#### 直し方（2つ）

**(1) 発話判定に「続いていること」を要求する。**
200ms（4フレーム）連続して初めて「発話あり」とする。一瞬の物音は通らない。

**(2) 停止時の短い末尾は、両エンジンが無音なら消す。**
`cutChunk` は `SILENCE_ABS_MIN_MS`(3秒) より前に切らないので、
**3秒より短いチャンクは録音停止時の末尾しか存在しない**。
そのうえ両エンジンが何も拾っていないので、消しても失う文字が無い。

なお、この表示は「再試行できます」と言っているが、再試行の仕組みは
**テキストを整形し直すもの**で、元の音声には触れない。
中身が空のこれを再試行しても何も起きないので、残す価値はそもそも低かった。

#### 消す条件（v0.18.10 時点）

前提（必須）:
- Gemini が空
- Web Speech も何も拾っていない

そのうえで、どちらかが言えるとき:
- (a) 検出器がこのチャンク中に発話音量を一度も見ていない
- (b) チャンクが 3 秒より短い（＝停止時の末尾しかありえない）

---

### 2026-08-31 v0.18.11 開始直後、まだ喋っていないのに区切っていた

v0.18.10 の実機結果（20:42）は良好。末尾の「（音声不明瞭・再試行可）」は消え、
二重表示も無く、要約も忠実。5本すべてが `頭=無音`。

残っていたのはログの1行目。

```
20:42:49 音声チャンク送信 長さ3.0秒 [幅5dB 判定不能 最長無音0ms 採用=webspeech]
20:42:53 沈黙のみのチャンクだったので表示しない
```

**録音開始から 3.0 秒ちょうど（＝ SILENCE_ABS_MIN_MS）で、まだ一言も喋って
いないのに区切っていた。** 表示はされないが Gemini への送信が1回まるまる無駄。

#### 原因

v0.18.4 で Web Speech を第二の手段にしたとき、`startLiveDisplay()` に
こう書いていた。

```js
state.liveLastActivityAt = Date.now();  // 0 のままだと開始直後に「無音」に見える
```

**これが余計だった。** `currentSilenceSignal` の `webActive` は
`!!state.liveLastActivityAt` を見ているので、0 のままなら
「Web Speech は使えない」と正しく判断される。現在時刻を入れたせいで
「使える」状態になり、そこからの経過（＝録音開始からの経過）が
そのまま「無音の長さ」として読まれていた。

3.0 秒で切れたのは、v0.18.9 で入れた「3秒以降なら2秒以上の無音で切る」に
`webMs = 3000` が当たったため。

#### 直し方

seed をやめて 0 のままにする。Web Speech が実際に何か聞き取るまで
この手段は使われない。

**レベル検出器で `canJudge()`（大きい側を知るまで判定しない）としたのと同じ原則。**
片方だけこの原則を破っていた。「何も聞いていない」は「無音が続いている」ではなく
「判断材料が無い」。

#### 直後の挙動

開始直後は両方とも使えないので、最長20秒まで区切らない。無言のまま20秒経てば
1本だけ送られて捨てられる（従来は3秒ごとに送っていた）。

---

### 2026-08-31 v0.19.0 録音音声の一時保管（全文やり直しの土台）

「録音後に全文をもう一度 Gemini に通して話者判別つきで作り直す」ための土台。
**この版では保管と設定までで、やり直し本体はまだ実装していない。**

#### 保存先の検討

| 候補 | 判断 |
|---|---|
| **IndexedDB** | ◎ 採用。Blob をそのまま入れられる。許可ダイアログ・設定が要らず、拡張でも Android Chrome でも iOS Safari でも同じコードで動く |
| ローカルフォルダ指定 | ✕ File System Access API は **Android Chrome に無い**。スマホ対応の道を最初に塞ぐ |
| Google ドライブ | △ OAuth が要る・毎回数十MBをアップロード・ネット必須。やり直しは端末から送るだけなので、外に出す必要がない |
| localStorage | ✕ 文字列専用・数MB上限（既存のセッション保存はこれ） |

スマホ化を見据えると保存先はここで決めてよく、**本当の壁は別**にある:
Android Chrome に拡張機能が無い（＝ HTML版を PWA にする形になる）、
Notion 送信が CORS で通らない（＝中継サーバが要る）、iOS は Web Speech が弱い。
**IndexedDB を選んでおけば、そのとき保管まわりは触らずに済む。**

#### 実装前に確かめたこと

「やり直し」は録音全体を1リクエストで送る必要があるので、先に2点を実測した。

1. **Files API のアップロードエンドポイントがブラウザから使えるか（CORS）**
   → `OPTIONS https://generativelanguage.googleapis.com/upload/v1beta/files` に
   `access-control-allow-origin` が返り、`x-goog-upload-*`（レジューム可能
   アップロードに必要なヘッダ群）も明示的に許可されていた。**使える。**
2. **インライン送信のサイズ上限**
   → 公式には **100MB**（私は当初「20MB」と誤って書いた。訂正済み）。
   ただし base64 で1.33倍に膨らむこと、長時間録音で上限に当たること、
   **Files API に上げた分は48時間で Google 側が自動削除される**ことから、
   やり直しは常に Files API を通す方針にする。上限値に依存しない作りになる。

#### 消えることを構造で保証する

やっさんの方針:

> 基本は保存したくない。文字起こしはいいけど録音はダメみたいな場合もあるよね。
> 2次利用がダメだと思うので基本はできるだけ早い段階で消す。
> ただしデータを残したい人もいるだろうからそこは設定で。

そのとおりで、**録音の保存に対する同意は文字起こしより狭いことが普通にある**。
既定は保持しない。保持する場合も、消し忘れが起きない作りにした:

- **タブが無くなった音声は、設定に関係なく必ず消す**（到達手段が無いのに
  残しているのは、ただの置き忘れ）
- 「タブを閉じたら消す」はクラッシュや強制リロードでは走らないので、
  **起動時の掃除を本体にする**（`sweepStoredAudio`）
- 保持をオフに切り替えたら、その場で全部消す（次の起動を待たない）
- **未知の設定値は「消す」側に倒す**（保持する側に倒さない）

判断は `audioRetentionExpired`（純関数）に集約した。ここを間違えると
「消したはずの録音が残る」という一番まずい壊れ方をするため。

| 設定 | 挙動 |
|---|---|
| `repass`（既定） | やり直しが終わったら消す。走らせないまま24時間で自動失効 |
| `close` | タブを閉じたら消す |
| `1d` / `7d` | その期間で消す |
| `manual` | 手動で消すまで残す（タブが消えれば消える） |
| `off` | 保持設定がオフ。残っている分も消す |

#### 設定 UI に正直に書いたこと

**この設定が制御するのは「端末に残すか」だけ。** Gemini モードは live の時点で
すでに音声を Google に送っている。ここを曖昧にすると利用者が誤解するので、
設定画面に明記した（`warn-hint`）。やり直しの実行時にも、録音全体が
アップロードされ48時間 Google 側に置かれることを出す予定。

なお **無料枠と有料枠でデータの扱いが違うはず**だが、そこは記憶で断言できない。
一般公開を視野に入れるなら規約を確認して書く必要がある。

#### ビットレート

従来はブラウザ既定（約128kbps）で、音声認識用途には過剰だった。
既定を **64kbps** にして設定から選べるようにした（32/64/96/128）。
保管サイズだけでなく live の送信量も減る。

| kbps | 90分 |
|---|---|
| 32 | 約21MB |
| **64（既定）** | **約43MB** |
| 128（従来） | 約86MB |

#### ついでに直したこと

`index.html` のキャッシュ避け `?v=` が **v0.17.4 のまま止まっていた**。
HTML版で古い JS が使われうる状態だったので 0.19.0 に更新し、
「更新のたびに manifest の version に合わせること」とコメントを入れた。

#### 残り（次の版）

- Files API へのアップロード（レジューム可能プロトコル）
- 話者判別つきの全文やり直しプロンプト
- 「この録音を話者付きでやり直す」ボタンと、送信先についての確認表示
- IndexedDB が使えない環境（プライベートモード等）の実機確認

---

### 2026-08-31 v0.19.1 診断ログの不備2件

v0.19.0 の実機ログ（23:28）で、ビットレート変更は確認できた。

```
長さ20.0秒 … 162134B   ← 従来 321974B のちょうど半分。64kbps が効いている
長さ 7.0秒 尻=無音 [最長無音2043ms]   ← 最短12秒より前の切れ目も機能
```

一方、ログ側に2つ不備があった。どちらも診断の質の問題。

#### ① 保持のオン/オフがログから分からない

`keepAudioChunk` は失敗したときしかログを出さないので、
**「録音の保持」の動作確認をログから判断できなかった。**
録音開始時に設定を1行出すようにした。

```
録音の保持: オン（タブを閉じたら消す）
録音の保持: オフ（端末に残しません）
```

#### ② 「最長無音」が誤解を招く値だった

3本目のチャンクは 8.9 秒なのに `最長無音3399ms` と出ていた。
無音の計測（`silentSinceMs`）はチャンクの区切りをまたいで続くため、
**前のチャンクから続いていた沈黙が合算されていた**。

判定にその継続を使うのは正しい（いま静かであることに変わりはない）。
問題はログの見え方で、「このチャンクの中に3.4秒の沈黙があった」と読めてしまう。
実際、この行を読んでタイムラインを引き直す羽目になった。

チャンク自身の長さで頭打ちにし、前から続いていた分があるときは `+` を付けた。

```
最長無音1356ms+   ← このチャンク内では1.36秒。前から続いていた分がある
最長無音2043ms    ← このチャンク内で完結している
```

---

### 2026-08-31 v0.19.2 実素材（ニュース8分）でのテスト

自分で喋るテストに限界を感じ、テレビのニュース解説を8分録音した。
**26チャンク・判定可24本／判定不能2本**、判定不能の2本はどちらも Web Speech の
予備経路が受けて切っている。内容の質は高く、見出しも要約も適切で捏造も無い。

#### 分かったこと1: 同音異義語は語彙ヒントの出番

```
対象となる強化の最大15%程度…他の強化などに上乗せ…強化の新設…独自強化
```

**「教科」が全部「強化」**になっていた。音だけでは決められない典型で、
v0.16 の語彙ヒントに `教科` を入れれば直る。

重要なのは、**自動抽出がこの誤変換を拾って悪化させていない**こと
（`強化` を語彙として送り返し続ける自己強化ループは起きていない）。
v0.16.1 で警戒した罠は踏んでいない。ただし自力で正すこともできないので、
分野が決まっている録音では手入力が効く。

#### 分かったこと2: 診断がまた足りなかった

「（音声不明瞭・再試行可）」が残っていたが、**なぜ残ったのかログから判断できない**。
消すかどうかは `hadSpeech` で決まるのに、その値を出していなかった。
v0.19.1 で同じ反省をした直後にまた同じ穴を踏んでいる。

`発話あり` / `発話なし` をログに追加した。

#### 分かったこと3: 重複検出が1文字足りずにすり抜けた

「乗り越えるべき」（**7文字**）が二重に出た。v0.18.8 の重複検出は偶然の一致を
避けるため **8文字未満を対象外**にしていたので、1文字足りずに通り抜けた。

一律に下げるとあいまい一致（2文字の並びの重なり）で誤検出が増える。
ただし**そのまま含まれている**場合は短くても確実なので、下限を2段階に分けた。

| 判定 | 下限 | 理由 |
|---|---|---|
| そのまま含まれている | **4文字** | 完全一致なら短くても確か |
| あいまい一致 | 8文字 | 短いと偶然当たる |

#### 強制区切りの割合について

8/26 が `尻=強制` だった。ニュースの読み上げは間が短く整っているので、
1000ms の切れ目が出にくい。実際「乗り越えるべき／多いですが」で分断が出た。

これは**しきい値を下げれば減るが、言いよどみで切る問題が戻る**トレードオフで、
v0.18.6 で実測から線を引いた場所を動かす根拠はまだ無い。
分断はミドル整形で繋ぎ直されるので、実害は限定的と判断して据え置く。

---

### 2026-08-31 v0.19.3 hadSpeech は「発話」ではなく「大きい音」を見ていた

v0.19.2 で「なぜ『（音声不明瞭・再試行可）』が残ったのか分からない」と書いた件、
やっさんの一言で確定した。

> ちなみにここは番組の終わりのジングルが流れてたよ

**音楽だった。**

#### 何が起きていたか

`speechInChunk` は「フロア+10dB を超える音が 200ms 続いたか」で判定している。
音楽は声の帯域に強い成分があって鳴りっぱなしなので、この条件を楽々通る。

v0.18.10 で「一瞬の物音」は 200ms 連続の条件で除外できたが、
**鳴り続ける音楽は素通りする**。つまりこの値は
**「発話があったか」ではなく「大きい音があったか」**でしかない。

#### 音楽判別は作らない

音楽と発話を分けるには、スペクトルの平坦さでは足りない（どちらも倍音を持つ）。
実用的なのは**変調スペクトル**（発話は 4〜8Hz の音節リズムを持つが音楽は持たない）
だが、ここで得られるものに対して大きすぎるし、このセッションでは実音声で
検証もできない。

#### 代わりに、表示の嘘を直した

従来の「（音声不明瞭・再試行可）」は2点で嘘をついていた。

| 文言 | なぜ嘘か |
|---|---|
| 音声**不明瞭** | 不明瞭なのではなく、そもそも言葉ではない |
| **再試行可** | 再試行はテキストを整形し直す仕組み。中身が空のこれを再試行しても何も起きない |

正直な文言に変えた。**何秒ぶんが文字にならなかったか**が分かれば用は足りる。

```
（この12.0秒は言葉として聞き取れませんでした。音楽や物音の可能性があります）
```

講義で30秒が聞き取れなかった場合も、これなら「そこに空白がある」ことと
その長さが伝わる。黙って消すより良い。

---

### 2026-08-31 v0.20.0 語彙辞書（プリセット＋自作）とフッター表示

実素材のテストで「教科」が全部「強化」になり、**分野が決まっているなら毎回
手で書くのは無駄**だと分かった。辞書として持ち回せるようにする。

#### 選定基準（一度作り直した）

最初「同音異義語で、その分野では表記が決まるもの」としたが、甘かった。

> 家庭 過程 課程 全て使うもんな
> ただし 家庭の教育 学習の過程 教育課程 文脈で判断はできるけどね

そのとおりで、`教育課程` は条件を満たしていない。教育の場では**家庭・過程・課程が
全部出る**ので、課程だけを渡すと家庭訪問の話も学習の過程の話も課程に寄る。
**改善どころか改悪**になる。

| 入れてよい | 入れてはいけない |
|---|---|
| その分野で同音異義語がまず出ないもの（教科／強化、所見／初見） | 同じ分野で複数の表記が両方使われるもの（家庭／過程／課程） |
| 音から想像しにくい専門語（褥瘡、稟議、冪等、抄録） | 一般語（対応・機能・進捗） |

同じ語でも分野で可否が変わる。`決裁` は行政では入れてよいが（決済がまず出ない）、
ビジネスでは入れてはいけない（決済代行・決済手段）。

`TERM_BLOCKLIST` に理由つきで固定し、テストで混入を防ぐ。行政のみ 所管・決裁 を
例外として許可する。

#### 合成の優先順位: 手入力 → 辞書 → 自動（上限50語）

**辞書を自動より前に置くのが判断のしどころ。** 録音中にプリセットへ切り替えるのは
「教科と書いてくれ」という明示的な訂正なので、上限で削られては意味がない。
自動抽出は放っておいても育つので、削られる側になってもらう。

#### フッターに常時表示する

> いま何の語彙が効いているかが見えていないと、間違ったまま録り続けてしまう

右下の拡大ルーラーと対になる**左下のピル**に出す。1本のバーにしなかったのは、
サイドパネルが狭く（実質360px前後）拡大ルーラーだけで200px超を使うため。
2つのピルなら狭いときに自然に上下へ逃げる。ズームピルと同じ位置切替
（チャット入力欄との重なり回避）とフェードに乗せてある。

```
教育・学校 / 手入力3 / 自動12      ← 効いているとき（明るく表示）
辞書なし                            ← 何も効いていないとき
```

**何も効いていないときにそう言うことも同じくらい大事。**

#### 辞書を選んでも自動辞書は消さない

20分喋ったあとの自動辞書は**実際に話された語**からできているので価値が高い。
プリセットに切り替えたときに捨てるのは損なので、
「いまの自動辞書 ＋ 選んだ辞書」で合成する。

#### 自動辞書を自作辞書に昇格できる

タグアイコン →「いまの語を辞書として保存」で、育った自動辞書と手入力の語に
名前を付けて保存する。**その場限りで消えていた学習が資産になる。**
保存した辞書はそのタブに自動で適用する（保存したのに効かない、を避ける）。

同名の辞書があれば足す（既存の語は消さない）。

#### 語彙ヒントは効くぶん効きすぎる

先に語を渡すと音声認識はその語を選びやすくなる。それが狙いだが、**言っていない語まで
選ばれる**ようになる。医療の辞書を入れると「しょけん」が常に「所見」になり、
「初見」と言っても直されうる。避けようがないトレードオフなので、
上の選定基準で影響を抑える。

---

### 2026-08-31 v0.18.7 要約が丸ごと捏造された（同じ失敗の3回目）

123字の「文字起こしのテストをしている」だけの録音から、こういう要約が出た。

> 本セッションでは、主に新規プロジェクトの立ち上げとそれに伴う組織体制、
> 役割分担について議論されました。…「リソース管理」機能を強化し…
> 会議室、備品…週次での定例会議を設定し…

**一言も話されていない。** タイトル生成は要約を材料にするので、放っておくと
タイトルまで巻き込まれる。

#### 原因

プロンプトを調べたが、例文の混入も文脈の漏れも無かった。むしろ逆で、
**プロンプトが入力に対して過剰な量を要求していた。**

```
- 冒頭に 3〜5 行の「# 概要」セクション
- 「## 主要ポイント」として箇条書きで重要トピックを 5〜8 個
- 各ポイントは前後の文脈を補って読みやすく
```

123字から「5〜8個の重要トピック」は取り出せない。作るしかない。
しかもシステムプロンプトが「講義・会議の文字起こし」と名乗っているので、
埋め草として一般的な会議の中身が出てきた。

プロンプトには **「元の内容に忠実に、推測や創作はしない」と書いてあった**。
効かなかった。

#### これは3回目の同じ失敗

| | 具体的な指示 | 効かなかった一般則 | 結果 |
|---|---|---|---|
| v0.17.1 | 「読みやすく整形して」 | 音声に無い内容は足さない | 断片の続きを創作 |
| v0.18.4 (誤診) | ― | ― | （測定の問題だった） |
| v0.18.7 | 「主要ポイントを5〜8個」 | 推測や創作はしない | 議事録を丸ごと創作 |

**目の前の具体的な指示は、一般則より強く働く。**
禁止を1行足しても、その禁止と矛盾する指示が残っている限り勝てない。

#### 直し方（v0.17.1 と同じ手）

禁止を足すのではなく、**圧力そのものを外し、入力の性質を教える**。

1. 数の要求を消す（「5〜8個」→「話された分だけ。数の目標は無い」）
2. 見出しの扱いを変える（「必要なら追加」→「**そこに書ける内容が実際に
   話されているときだけ**追加。無ければ見出しごと省く」）
3. 「前後の文脈を補って」を削除（内容を足す許可に読める）
4. 短い入力が異常ではないと明示する
   （v0.17.1 で「この音声は断片です」と教えたのと同じ）
5. ユーザープロンプトに**実際の文字数**を入れる。「短い」と言葉で言うより数字が効く

#### テスト

`t-summary.js` で3段階すべてのプロンプトを組み立てて検査する。
「1〜2行でよい」のような**許可**と「5〜8個」のような**要求**を区別して判定する
（区別しないと、対策として入れた許可の文言まで違反として拾ってしまう）。
**変更前の gemini.js に当てて実際に落ちることを確認済み。**

#### 残った類似箇所

タイトル生成の「10〜20文字」は長さの制約であって内容量の要求ではないので残した。
ただしタイトルは要約を材料にするため、**要約が捏造されるとタイトルも巻き込まれる**。
要約側を直せば連鎖は止まる。

---

#### 残ること

チャンクどうしを数秒**重ねて**送る案は入れていない。MediaRecorder の stop/start では
重複した音声を取り出せないため、実装するなら AudioWorklet で PCM を自前バッファする
別方式になる。無音区切りで足りるかを実機で見てから判断する。

---

### 2026-08-31 v0.17.4 画面に出ている段落がコピーから消えていた（既知バグの取り残し）

やっさんの報告「コピーボタンの結果とウィンドウ内の内容が違うかも」。

#### 私は一度、間違った説明をした

最初「`#interim`（未確定表示）は `#confirmed` の兄弟なのでコピー対象外。
コピー時点ではまだ未確定だったのでは」と答えた。**やっさんに否定された。**
「文章の中盤だよね？すでに確定されてたんじゃないかな。とっくに録音は終わっている状態」。

その通りで、録音済み・確定済みで再現する以上、未確定の話ではなかった。
**画面のスクリーンショットに手がかりがあった**（その行だけ前の段落との間に空行がない）。
説明を思いついた時点で確かめずに答えたのが誤り。

#### 本当の原因

`setParagraphContent()` は refined テキストを空行で分割し、
**1つの `.paragraph` の中に `.p-body` を複数作る**。

```html
<div class="paragraph refined">
  <h2>文字起こしテスト</h2>
  <div class="p-body">あ、あ、テスト…現在試しているところです。</div>
  <div class="p-body">先ほどはWeb Speechがカタカナ</div>   ← 2つ目
</div>
```

ところが `getConfirmedText()` は

```js
const h2 = p.querySelector('h2');
const body = p.querySelector('.p-body');   // ← 最初の1つだけ
if (h2 && body) return `## ${h2...}\n\n${body.innerText}`;
```

**見出しのある段落では、2つ目以降の本文が黙って捨てられていた。**
見出しが無い段落は `p.innerText` を返すので無事。だから「たまに1段落だけ消える」
という気づきにくい形で出ていた。

#### これは既知バグの取り残しだった

`app.js` の別の場所（全体整形の入力を作るところ）には、**まったく同じ問題への修正が
すでに入っていた**。コメントまである。

```
// querySelector('h2') だと先頭1つしか取れず、残りを丸ごとロストする。
// 全子要素を走査して h2/.p-body/その他 を順序通りに並べ直す。
```

**同じ形のバグを1箇所直して、もう1箇所を見逃していた。**

#### 影響範囲（正確に）

`getConfirmedText()` を通るものだけが影響を受ける。

| 影響あり | 影響なし |
|---|---|
| コピー（プレーン / Markdown）| **Notion 送信**（`session.transcript` の HTML を読む）|
| AI 要約の入力 | HTML 保存（同上）|
| 質問（チャット）の資料 | |
| AI タイトル生成 | |
| 文脈の自動抽出 (v0.16.1) | |

私は最初「Notion にも影響する」と言ったが**これは誤り**。確認したら Notion は
`notionBlocksFromHtml(session.transcript)` で HTML から組むので全部拾えていた。

#### 教訓

- **同じ形のバグは他にもある。** 1箇所直したら、同じパターンを grep して回る。
  今回は既存の修正コメントが残っていたのに、隣の関数が見逃されていた
- **説明を思いついても、確かめるまで言い切らない。** 「未確定だったのでは」は
  もっともらしかったが外れていた。やっさんに否定されて初めて DOM を見に行った
- **やっさんの「違うと思う」は正しい。** 使っている人の観察は、
  こちらの推論より事実に近いことがある

---

### 2026-08-31 v0.17.2 / v0.17.3 断片の繋ぎ直しと、確定待ちのまま保存する事故

#### v0.17.1 は効いた。ただし代償が出た

やっさんの実機テストで、チャンクの区切りが文の途中に落ちたケースが再現した。

```
…現在試しているところです。

になっていましたが、今回はアルファベット表記になっているようです。
```

**今回は Gemini が続きを創作せず、断片をそのまま出した。** v0.17.1 は意図どおり効いた。
代わりに、頭の欠けた段落がそのまま残るようになった（捏造よりはるかにマシだが読みにくい）。

#### v0.17.2 統合の段で繋ぎ直す

短チャンクが 3 つ溜まると `consolidateShortChunks()` が `refineWithGemini` でまとめて整形する
仕組みが元からある。ところが**「これらは同じ発話を機械的に切った断片の並びだ」と
伝えていなかった**ので、別々の段落として扱われていた。

`refineWithGemini` に `joinFragments` を足し、統合の段でだけ次を渡すようにした。

```
以下は、同じ連続した発話を一定時間で機械的に区切った断片の並びです。
- 文の途中で切れている断片どうしは、つないで元の1文に戻すこと
- つないでよいのは、両方の断片が実際にここにある場合だけ。
  片方しか無いものに言葉を足して文を完成させてはいけない
```

**繋ぐのは再構成、足すのは創作。** 両方の断片が手元にあるかどうかが境界線で、
これを明示しないと v0.17.1 で止めた捏造が別の場所から戻ってくる。

#### v0.17.3 確定待ちのまま Notion に保存して閉じると消える

やっさんの「コピーボタンの結果とウィンドウ内の内容が違うかも」という報告から辿り着いた、
**より重い問題**。

コピーの差分自体は仕様どおりだった。`#interim`（未確定のライブ表示）は `#confirmed` の
**兄弟**要素なので、`getConfirmedText()` の対象に入らない。画面には出ているがコピーには入らない。

問題はその先。**`uploadSessionsToNotion` が `audioInFlightCount` を一切見ていなかった。**

1. 録音停止直後（Gemini の返事待ちが数件ある状態）で Notion に保存
2. 「閉じる」を押す → タブが削除される
3. **返事待ちだった文字起こしは保存もされず消える＝永久に失われる**

やっさんの普段の使い方（保存して閉じる）でそのまま起きる。
`ensureTranscriptSettled()` を追加し、保存前に

- 録音中なら「止めてから保存を」と確認
- 確定待ちがあれば、待つ（最大30秒）か中止かを選ばせる。待って確定したら取り込んでから保存

とした。

#### 教訓

- **「表示されているもの」と「保存されるもの」がずれる設計は、
  それ自体がバグの温床。** 片方だけ見ていると気づけない
- **消える操作（閉じる・削除）の前には、未完了の処理が無いか必ず確かめる。**
  今回は「保存 → 閉じる」という一連の流れの中に穴があった
- やっさんの「〜が違うかも」という小さな違和感から、
  データが消える経路が見つかった。**違和感は最後まで辿る価値がある**

---

### 2026-08-31 v0.17.1 言っていない文が出た（断片を「完成させて」しまう）

やっさんの実機テストで発覚。**「はないので、このまま進めていきたいと思います。」は
言っていない**という報告。

#### 原因は特定できた

やっさんが実際に言ったのは「でも実害**はないので**大丈夫ですが」。
チャンクの区切りが「実害 / はないので」の間に落ち、**次のチャンクの音声が
文の途中「はないので」から始まった**。Gemini はそれを見て、
**自然な文になるよう続きを創作した**。

v0.16.0 の設計メモに「文脈に引きずられて言っていないことを書くのが最悪の壊れ方」と
書いていた、まさにそれが起きた。**予測していた失敗が現実になった。**

#### なぜ補完禁止ルールが効かなかったか

`buildNoInventionRule()`（「音声に無い内容は絶対に足さない」）は入れてあった。
それでも起きたのは、**モデルが「これは断片だ」と知らなかった**から。

プロンプトは「入力音声を文字起こしし、**読みやすく整形**してください」と言っている。
文の途中で終わっている入力を渡されたモデルにとって、
「整った文にする」＝「続きを補う」は指示に従った行動に見える。
一般則（補完禁止）より、目の前のタスク（整形）のほうが強く働いた。

#### 対処：入力の性質を先に教える

抽象的な禁止を足すのではなく、**入力が何であるかを説明する**方向にした。

```
■ 最重要：この音声は「断片」です
これは長い録音を一定時間で機械的に切り出したものです。
文の途中から始まり、文の途中で終わるのが普通です。それが正常な入力です。
- 途中から始まっていても、聞こえたとおりに書く。前を推測して補わない
  （例: 音声が「はないので大丈夫ですが」で始まるなら、そのまま書く。
   「実害」などの頭を勝手に足さない）
- 途中で終わっていても、続きを作らない。聞こえたところで止める
  （文として不完全なまま終わってよい）
```

実際に起きた事例をそのまま例に使った。あわせて「直前の文脈」のラベルを
「表記の参考のみ。**ここから話を続けて書かないこと**」に変え、
文脈が続きを書く材料として使われないようにした。

#### 教訓

- **「やるな」より「入力はこういうものだ」。** 禁止ルールは、モデルが状況を
  誤解していると効かない。誤解のほうを解くのが先
- **整形を頼むと、モデルは整った出力を作ろうとする。** 断片を渡すなら
  「不完全なまま返してよい」と明示的に許可しないと、勝手に完成させる
- 予測していた失敗でも、実際に踏むまで対策が足りているかは分からない。
  **実機で使ってもらう価値はここにある**

#### 残る根本原因

そもそも**チャンクを時計で機械的に切っているから**文の途中で切れる。
無音を検出した位置で切る、あるいは前後を数秒重ねて送れば、断片そのものが減る。
プロンプトでの対処は対症療法であって、根治ではない。

---

### 2026-08-31 v0.17.0 Gemini モードでもリアルタイム表示（Web Speech 並走）

やっさんの指摘：「Gemini モードにしていてもリアルタイムで未確定の文字がパラパラ出て、
ある程度したら Gemini により確定される、って思ってたけど違うのかな」。

**その通りで、まだ作っていなかった。** マイク同時利用が可能なことを確かめただけだった。

| | 変更前 | 変更後 |
|---|---|---|
| WebSpeech モード | 未確定→確定、どちらも Web Speech | 変更なし |
| Gemini モード | **未確定表示なし**。12 秒ごとに一気に出る | **Web Speech を並走させて未確定表示** |

#### 時間軸を合わせなくて済む設計

Web Speech と Gemini は別の時計で動くので、素朴にやると「どの未確定文がどの Gemini 結果に
対応するか」の突き合わせが必要になる。これを避けるため、**チャンクの区切りで受け渡す**形にした。

```
喋る → Web Speech の文字が #interim にパラパラ出る（未確定）
     → チャンクの区切り（12秒）でその文を段落に固定（まだ未確定の見た目）
     → その音声の Gemini 結果が返ったら、その段落が置き換わる（確定）
```

区切りで `takeLiveFinals()` が「そこまでに Web Speech が確定した分」を取り出して
そのチャンクの placeholder に載せる。**対応関係が区切りで自明になるので突き合わせが要らない。**

区切り時点でまだ未確定だった語は次の窓に持ち越される（Web Speech が後で確定するため）。
1 チャンクぶんずれるが、重複も欠落もしない。

#### transcript には書かない

`buildRecognition()` は `appendRawChunk` や字幕バッファと密結合しているので流用せず、
**表示専用の `buildLiveDisplayRecognition()` を別に作った**。こちらは transcript に一切書かない。
Web Speech が落ちても録音は続く（本体は Gemini 側）。

#### Gemini が失敗しても Web Speech の結果を捨てない

実装中に気づいた穴。従来のコードは失敗時に placeholder を
`[文字起こし失敗: …]` で上書きしていた。並走させると、**聞き取れていた文字があるのに
それを捨てて「失敗」とだけ残す**ことになる。最悪の結果なので、
失敗時・Gemini が空を返した時とも、Web Speech の結果を残して注記を付ける形にした。

#### 停止時の順序

`stopLiveDisplay()` で `liveFinals` を消してはいけない。停止時に MediaRecorder が
最後のチャンクを吐くので、その未確定表示に使う。次の録音開始時に
`startLiveDisplay()` が空に戻すので溜まりっぱなしにもならない。

#### 設定

「リアルタイム表示を使う（Web Speech を並走）」チェックボックス（既定 ON）。
マイクと通信を二重に使うので、切りたい人のために残す。

#### 実機で確かめること

このセッションでは動作を確認できない（マイクとブラウザが要る）。
やっさんの環境で、未確定表示が出るか・確定で置き換わるか・二重に出ないかを見てもらう。

---

### 2026-08-31 v0.16.3 例文の「分野の偏り」は UI だけの話ではなかった

やっさんの指摘：「個人情報がというよりか、例が教育に偏っているというのがなんかね。
もし自分がこういうアプリを開いて『宇宙環境における閉鎖系生態系生命維持システム（CELSS）
構築特論』みたいなのばかり出ていたら、う、うーん てなるかな」。

私は最初これを個人情報の問題として受け取り、実在の人名を消す方向で直した。
**やっさんが気にしていたのはそこではなかった。** 一般のユーザーが最初に見る例が
特定分野に寄っていると「これは自分向けのアプリではない」と感じる、という話だった。

#### 直したもの

UI のプレースホルダを、**分野を持たない形**にした。
「経済学の講義」のように分野を1つ選ぶのではなく、「講義 / 会議 / 打ち合わせ /
インタビュー」という場面の種類だけを並べる。語彙の欄も具体的な用語ではなく
「製品名・サービス名 / 社名・団体名 / 略語 / 人名」というカテゴリにして、
**入力の形式（1行1語）を教えつつ分野を示さない**ようにした。

#### UI だけの話ではなかった

指摘を受けて見直したら、**自動抽出のプロンプトの中にも分野の偏りが入っていた。**

```
- 音声認識の誤変換と思われるものは、正しい表記に直して入れる
  （例「不当校」と書かれていても、文脈から「不登校」が正しいならそう入れる）
- 一般語（会議、今日、先生、みなさん等）は入れない
```

これはモデルに送られる文字列なので、**拾う語が教育寄りに引っ張られる可能性がある**。
見た目の問題ではなく機能の問題だった。同音異義語の例を「機関／期間」に、
除外する一般語から「先生」を外した。

#### 教訓

- **例を書くときは「どの分野の人が読むか」を考える。** 開発者が身近な題材で書くと、
  そのまま製品の顔になる
- **例はプロンプトの中にも潜んでいる。** UI の文言だけ直しても、モデルに渡している
  例が偏っていれば出力が偏る。見た目と機能の両方を見る
- やっさんの指摘の**真意を取り違えた**。「実在している」と言われて個人情報だと
  判断したが、本当の懸念は「分野が偏って見える」ことだった。
  聞き返さずに動くと、正しく直したつもりで的を外す

---

### 2026-08-31 v0.16.1 文脈の自動抽出（メモが空でも効かせる）

やっさんの質問「メモペインは書かないこともあるんだけど、その場合は？」への答え。
**書かなくても成立させる**。記録が溜まってきたら、そこから語彙と議題を拾い直す。

| メモの状態 | 語彙・議題の出どころ |
|---|---|
| 書いてある | メモを優先（人が書いたものが最終権限）＋ 記録から補う |
| 空 | 記録から自動抽出。最初の数分は文脈なし、進むほど効いてくる |

#### 最大の罠：誤変換の自己強化ループ

文字起こし自体に誤りが含まれる。**頻度で語を拾うと、序盤の誤り（「不登校」→「不当校」）を
そのまま語彙として送り返し、以降ずっとその誤りを書き続ける。** 誤りが自分を強化する。

対処は 3 つ。

1. **頻度カウントをしない。** モデルに「この分野の用語として正しい表記」を判断させ、
   プロンプトで明示的に「音声認識の誤変換と思われるものは正しい表記に直して入れる」と指示する
2. **抽出結果を画面に出す。** 文脈モーダルに「自動で拾った内容」欄を置き、
   「上の欄に取り込む」で手入力側へ移して直せるようにした。隠れて使われるのがいちばん危ない
3. **手入力が常に優先。** `getSessionContextForAi()` は手入力の語を先に並べ、
   自動抽出は重複を除いて後ろに足すだけ

#### 実装

- `extractContextWithGemini()`：JSON で `{terms, topicPath, flow}` を返させる。
  temperature 0.1、記録の末尾 4000 字だけを渡す（全文を毎回送っても意味がないうえ、
  「いま何の話か」は直近で決まる）
- **モデルの出力はそのまま信じない。** 語は 20 字以内・最大 20 件、topicPath は最大 3 階層、
  flow は 120 字で切る。JSON として読めなければ空を返して黙って諦める
- 録音中 30 秒おきにチェックし、前回から 2 分経っていれば実行。停止時にも 1 回だけ強制実行
  （次に同じタブで録音を再開したとき、いきなり文脈が効く）
- Web Speech モードと Gemini モードの両方で走る

#### 途中で見つけた不具合：サニタイザが JSON を壊す

テスト中に、有効な JSON が parse できない現象が出た。原因は `_callGemini` の
`_stripThinkingArtifacts`。**「同じ短句が 15 回以上続いたら省略マーカーに置換」**する
繰り返しハルシネーション対策が入っており、これは散文には正しいが、
**構造が決まっている JSON に当てると壊れて読めなくなる**。

→ `_callGemini` に `skipSanitize` を足し、JSON を期待する呼び出しでは通さないようにした。

今回テストを踏んだのは `'あ'×200` という人工的な値だったが、
実際の応答でも引用や語の並びで踏みうる。**新しく JSON 応答の呼び出しを足すときは
必ず `skipSanitize: true` を付けること。**

#### 検証

jsdom で異常な応答を流して確認した。

- 正常な JSON → そのまま通る
- 空要素・20 字超の文・20 件超・4 階層の topicPath・200 字の flow
  → すべてこちら側で切り詰められる
- JSON として壊れた応答（```json + 壊れた中身）→ 空を返して落ちない
- 記録が 200 字未満 → **API を叩かない**（fetch が呼ばれないことを確認）
- `responseMimeType: application/json` / `temperature: 0.1` が実際に送られている

---

### 2026-08-31 v0.16.0 文字起こしに「この録音の文脈」を渡す

やっさんの相談：「せっかく AI で文字起こしをしているのだから、なんの話題なのかを
捉えながら文字起こししてくれると精度が上がるのでは」「Notta に負けない工夫はないか」。

#### なぜ効くのか

日本語の音声認識でいちばん誤るのは**同音異義語と固有名詞**で、これは音だけでは
原理的に決められない。「かてい」は 家庭／課程／過程、「きかん」は 期間／機関／器官。
「いまは不登校対策の話をしている」と分かっていれば **家庭** が選べる。
要するに商用サービスの「カスタム辞書」と同じことをやる。

**実測例がある。** マイク同時利用テスト（下記）で Web Speech が
「今 Web スピーチと**ジムニー**が両方」と書いた。Gemini が「ジムニー」になった。
語彙を先に渡してあれば防げる類の誤りで、この機能の価値がそのまま出ている。

#### 逆効果になる境界（設計上いちばん大事なところ）

文脈を渡しすぎると、モデルは**「聞こえた内容」ではなく「文脈から予想される内容」を
書き始める**。音声が不明瞭なときに、言っていないことを自然な文章で埋める。
文字起こしとしては最悪の壊れ方で、しかも読んで気づけない。

守る原則を 2 つセットで実装した。

1. **文脈は「語彙」として渡し、「筋書き」としては渡さない。**
   文脈ブロック自身の末尾に「これは語彙のヒントであって台本ではない。ここに
   書かれていることを音声より優先したり、書かれている内容を補ったりしないこと」を必ず付ける
2. **補完禁止を毎回明示する**（`buildNoInventionRule`）。
   「音声に無い内容は絶対に足さない」「聞き取れない箇所は [不明瞭] と書く」

#### 入れたもの

- `gemini.js` に `buildContextBlock()` / `buildNoInventionRule()`。
  **文脈が空なら空文字列を返し、プロンプトに何も足さない**（従来と完全に同じ形になる）
- セッションごとに `session.context = { field, speakers, terms }`。
  古いセッションには読み込み時に空の器を足す
- 新しいタブは設定の既定値を引き継いで始まる（同じ講義を何度も録るため）
- ツールバーに「文脈」ボタン（tag アイコン）→ モーダルで編集
- 「メモから取り込む」：メモの見出し・箇条書き・チェック項目から**短い語だけ**拾う。
  30 字を超える行は文であって語彙ではないので除く（長文を渡すと台本として
  読まれる危険が増す）
- 音声文字起こしと整形の**両方**に渡す。Web Speech モードでも整形の段で
  誤変換を直せることがあるため

整形側の `SYSTEM_PROMPT` 本体は触っていない。過去にプロンプト書き換えの連鎖で
事故った記録（v0.13.6→7→8）があるので、文脈ブロック側に自己申告を持たせる形にした。

#### 検証

jsdom で実際にリクエストを組み立てて確認した。

- 文脈あり：整形・音声とも、文脈ブロックが先頭に載る
- **文脈が空／空白のみ：何も足さず、v0.15.x までと同一のプロンプトになる**
- 補完禁止の 2 行は、文脈の有無にかかわらず system に必ず入る

#### まだやっていないこと

- メモが空のときの**自動抽出**（文字起こしから語彙と議題を拾う）。次の段。
  誤変換をそのまま学習して自己強化するループが最大の risk なので、
  単純な頻度カウントではなく AI に判断させる形にする予定
- 議題トラッカー（いま大項目のどこを話しているか）
- 話者判別、停止後の全体再文字起こし

---

### 2026-08-31 マイク同時利用の検証（WebSpeech + MediaRecorder）

「WebSpeech で即時表示しながら、裏で録音データを Gemini に送る」構成が成立するかの検証。
`mic-test.html` / `mic-test.js` を作り、やっさんの実機（拡張の中）で実行してもらった。

**結果：同時利用できる。** `MediaRecorder` を先に開始し、あとから Web Speech を
重ねても弾かれなかった。並行構成に進んでよい。

副産物として分かったこと：

- 6 秒チャンクが約 96KB（≒128kbps）。本体の無音判定は **400 バイト**なので、
  チャンク全体の 0.2% という極端に低い値。**「小さい声が捨てられている」という
  私の見立ては外れ**だった。逆に、無音スキップが実質効いていない（完全な無音でも
  API を叩く）という別の話になる。精度ではなくコストの問題なので優先度は低い
- チャンクの区切り（6.9s / 12.9s / 18.9s）と Web Speech の確定（14.5s / 17.8s / 21.7s）が
  まったく噛み合っていない。時計で機械的に切っているのが数字で見えた

---

### 2026-08-31 v0.15.2 追記の 404 を再試行しておらず、ノートが途中で切れうる

やっさんの報告：診断ログに `[notion] アップロード失敗: …(06/15 16:13) 保存先が見つかりません。
Notion 側でデータベースのページを「…」→「コネクト」から接続してください` が出ていた。
いつ出たか本人にも分からず、「キャンセルしたせいでは」という見立てだった。

#### 切り分け

- **キャンセル説は否定できる。** このメッセージは Notion が返した HTTP 404 の翻訳。
  通信中断なら fetch が reject して「Notion に接続できませんでした」になる
- やっさんに Notion 側を見てもらったところ、**そのノートは存在し、中身も入っていた**

「失敗したのにノートはある」で見立てが変わった。`notionCreateNote` は 2 段階で作る。

```
1. POST /pages                    → タイトルだけのノート
2. PATCH /blocks/{id}/children    → ペインをトグルとして流し込む（100 ブロックずつ）
```

**1 は成功、2 のどこかで 404** なら、ノートは存在し中身も途中まで入る。この形と一致する。

#### 根本原因（私のリトライ条件の穴）

作ったばかりのページ／トグルに追記するとき、Notion 側の反映が追いつかず一瞬 404 を
返すことがある。ところが私のリトライは **429 と 5xx だけ**で、404 は即 throw していた。

結果：
- 後半の追記だけ落ちて **ノートの中身が途中で切れる**
- しかも表示されるメッセージが「コネクトから接続してください」で**完全に的外れ**
- 中身が入っているように見えるので、**欠けに気づけない**

#### 対処

- `notionRequest` に `retryNotFound` オプションを足し、**`/blocks/.../children` への追記だけ**
  404 もリトライ対象にした。保存先の取得やページ作成の 404 は本物の可能性が高いので対象外
- 404 のメッセージを呼び先で出し分ける。`/blocks/` なら
  「ノートは作られましたが、中身の書き込みに失敗しました…中身が途中で切れていないか確認してください」
- 失敗ログに**保存先の名前・ID・日付列**を残す。以前はタブ名と理由だけで、
  あとからログを見ても**どの DB に送ろうとしたのか分からなかった**

jsdom で「追記が 2 回連続 404 → 3 回目で成功」を再現し、
修正前なら欠けていた後半のチャンクが最後まで送られることを確認した。

#### この仮説は外れだった（追記・2026-08-31）

やっさんに確認してもらったところ、**そのノートの文字起こしは末尾まで入っていた**。
追記が全部成功していたなら、上の「後半の追記が 404 で落ちた」という筋は成り立たない。

**当該エラーの原因は未特定のまま。** 分かっていることだけ書いておく。

- Notion が返した本物の HTTP 404（通信中断ではない）
- そのタブのノートは Notion に存在し、中身も完全
- 失敗したタブは閉じない仕様なので、タブは手元に残っていた

この2つと矛盾しない読み方は「**同じタブを2回上げようとして、1回目がページ作成の
段階で 404、タブが残ったので後から上げ直して成功した**」。ノートが 1 枚だけなら
この筋が合う。ただし裏は取れていない。

**次に同じことが起きたら**、v0.15.2 で入れた失敗ログ（保存先の名前・ID・日付列）を見る。
それが無かったせいで今回は絞り込めなかった。

なお v0.15.2 の修正自体は仮説とは独立に有効。作った直後のノートへの追記で 404 が
返れば実際に中身が途中で切れるので、再試行を入れたこと自体は残す。

#### 教訓

- **リトライ条件は「どのステータスか」ではなく「どの呼び先か」で決まる。**
  同じ 404 でも、保存先が無いのか、作った直後で反映待ちなのかで意味が正反対
- **エラーメッセージを 1 つの原因に決め打ちで書かない。** 「コネクトしてください」は
  親切のつもりだったが、別経路の 404 では嘘を言っていた
- **失敗ログには「何に対して」失敗したかを必ず残す。** タブ名と理由だけでは追えない

---

### 2026-08-31 v0.15.1 メモの空行が入らない／入れ子リストが1行に潰れる

#### メモペインで空行が入れられない

やっさんの報告と画像：箇条書きの途中に空行を入れたいが入らない。
「・のあと何も文字を書かずにエンターを押したら以降の行全体が下がるようにしてほしい」。

原因は「空の li で Enter」の処理（`els.memo` の keydown）。

```js
list.parentNode.insertBefore(newBlock, list.nextSibling);   // ← リスト全体の「後ろ」
```

**どこで押しても空行をリスト全体の後ろに置いていた。** リストの途中で押すと
空行が末尾に飛び、以降の行は下がらない。入れ子リストでは `list.parentNode` が
親の `<li>` なので、空行が親項目の中に潜り込む事故も起きていた。

直した挙動：

| 押した場所 | 結果 |
|---|---|
| リストの末尾 | 従来どおりリストを抜けて、その下に空行 |
| リストの途中 | **その位置でリストを分割し、間に空行を挟む**（以降の行が下がる）|

番号付きリストは分割すると番号が 1 に戻ってしまうので、
後半の `<ol>` に `start` を付けて番号を継続させる（1,2 → 空行 → 3,4）。

jsdom で 6 ケース（途中／末尾／先頭／番号付き／入れ子／空 li だけ）を実際に流して確認した。

#### 入れ子の箇条書きが 1 行に潰れていた（v0.14.2 からの不具合）

上の修正をテストしている最中に見つけた、**私が書いた Notion 変換の不具合**。

```
<ul><li>親項目<ul><li>子項目A</li><li>子項目B</li></ul></li></ul>
  → "親項目子項目A子項目B"   ← 1つの箇条書きに連結されていた
```

`notionRichTextFromNode` が子孫を全部たどるので、入れ子リストの文字まで
親項目の本文に混ざっていた。やっさんのメモは実際にこの構造（○ の入れ子）なので、
気づかなければ崩れたノートが Notion に溜まり続けるところだった。

対処：
- `notionRichTextFromNode` は `ul` / `ol` を本文から除外する
- `notionListBlocks()` を新設し、入れ子は Notion の `children` として組み立てる
- Notion は 1 リクエストで「ブロック + その子」の 1 段までしか送れない。
  このアプリは ペイン=toggle の中に入れるので `toggle → 項目 → 入れ子項目` が上限。
  それより深い段は内容を落とさず同じ高さに並べる

#### 教訓

- **「ついでに直す」ときこそテストを書く。** 入れ子の潰れは、メモの空行修正を
  jsdom で検証していなければ見つからなかった。手で確かめていたら通り過ぎていた
- **やっさんが画像を送ってきたら、その構造をそのままテストケースにする。**
  今回は画像の入れ子箇条書きをテストに入れたから不具合が出た

---

### 2026-08-30 v0.15.0 hidden 属性が CSS の display に負けていた事故

やっさんの報告：「一括送信を途中で止めて動作は止まったように見えたのに、
後から見たら最後までアップされてた。途中では止まらないのかな？」

#### 二重の原因

**1. 中止する手段を作っていなかった。** v0.14.2 の進捗ダイアログには中止ボタンが無く、
背景クリックにも Escape にも反応しない作りだった。これは単純な実装の抜け。

**2. それ以上に悪い方：`hidden` 属性が効いていなかった。**
進捗ダイアログのフッタは `<div class="modal-footer" id="notion-progress-footer" hidden>` と
書いてあり、保存が終わるまで隠れているつもりだった。ところが

```css
.modal-footer { display: flex; }   /* ← これが勝つ */
```

ブラウザ既定の `[hidden]` は `display: none` だが、**要素側で display を明示していると
そちらが勝つ**。つまり保存中もずっと「閉じる」「残す」が見えていた。
しかも `onclick` は `notionFinish()` の中で初めて代入するので、
**2回目以降の保存では前回のハンドラ（`notionProgressClose`）が残っている**。
保存中にそれを押す = ダイアログだけ閉じてループは走り続ける。
やっさんの「止まったように見えたが最後までアップされていた」はこれで完全に説明がつく。

同じ理由で `.btn { display: inline-flex }` の要素も `el.hidden = true` で消えない。

#### 対処

`style.css` の冒頭に一括で入れた。

```css
[hidden] { display: none !important; }
```

既に `hidden` を使っていた `.empty-hint` 系は display を明示していないので、
このルールを足しても挙動は変わらない（追加前に全箇所を確認済み）。

そのうえで本来の要望どおり**中止ボタン**を付けた。押した瞬間には止めず、
**送信中のノートを1件送り終えた区切りで抜ける**。途中で切ると Notion 側に
中身が欠けたノートが残るため。1件だけの保存では区切りが来ないので中止ボタンは出さない。

#### 教訓

- **`el.hidden` を使うなら、その要素に `display` が指定されていないか確かめる。**
  「隠したつもり」は動作で確認しないと分からない
- **ハンドラを後から代入する UI は、押せない状態にしてから出す。**
  前回の実行で付いたハンドラが残っていると、意図しないタイミングで発火する
- やっさんの「〜に見えたけど実際は違った」という報告は、たいてい
  **表示と実体がずれている**という意味。表示側を疑う

---

### 2026-08-30 CRLF 事故（差分1万行に見えたが中身はゼロ）

やっさんが v0.14.x を試すために `git checkout` したところ、こう言われて止まった。

```
error: Your local changes to the following files would be overwritten by checkout:
        PROJECT_DESIGN.md / app.js / index.html / manifest.json / style.css
```

`git status --short` で 10 ファイルが ` M`。`git diff --stat` を見ると **14566 insertions /
14566 deletions**、`app.js` だけで 12388 行。2 ヶ月分の未コミット作業が埋まっているように見えた。

#### 判断の分かれ目

ここで `git reset --hard` や `git checkout -- .` を打っていたら、本物の作業だった場合に
復元できなかった。**打つ前に中身を確かめたのが正解だった。**

見るべきだったのは数字。**insertions と deletions が完全に同数**で、しかも全ファイルが
ほぼ全行。これは「全行が 1 対 1 で置き換わった」= 改行コードの差分の形。

```bash
file app.js                          # → "with CRLF line terminators"
git diff --stat --ignore-cr-at-eol   # → 出力なし = 中身の差分はゼロ
```

これで中身の変更が 1 行も無いことが確定したので、`git checkout -- .` で捨てて解決した。

#### 原因と再発防止

Windows 側のツールがソースを CRLF で保存したことによる。変更されていたのは 10 ファイルで
`icons.js` `serve.js` `background.js` `main.js` `package.json` は無傷だったので、
全ファイルではなく「そのツールが開いたファイルだけ」だった。

`.gitattributes` に `* text=auto eol=lf` を置いて再発を止めた。以後は Windows 側から
CRLF で保存されても git の中では LF に正規化されるので、差分として出てこない。
`*.bat` だけは Windows のバッチなので `eol=crlf` にしてある。

#### 教訓

- **巨大な差分を見ても、まず数字を読む。** insertions == deletions は改行コードのサイン
- **確認する前に破壊的なコマンドを打たない。** `--ignore-cr-at-eol` で 1 秒で判別できる
- 判別がつかないときは捨てずに一時ブランチへコミットして退避する。
  stash より、名前の付いたブランチのほうが数ヶ月後の自分に見つけてもらえる

---

### 2026-04-26 v0.13.31 design-system を読まずに number input を入れた事故

v0.13.31 の設定 UI として `<input type="number">` を素のまま追加。
スピナー（↑↓）がネイティブ装飾のまま出てしまい、やっさんから

> もーこれも何度も何度も言ってるけど！こういう↑↓とかのデザインは統一して！
> デザイン.mdに書いてあるはず！

と指摘される。**「何度も何度も」= 私が過去に同じ指摘を受けて記録しなかった証拠**。
ルール11違反の典型例。ここに書く。

**該当のデザインルール**（`~/manage/design-system/principles.md` line 96-106）：

> ネイティブブラウザスタイル（白い select、青い input border）は**必ず塗り替える**。
> - ネイティブ装飾（`select` の矢印 / number スピナー）は `appearance: none` で消して、SVG で自作
> - やってはダメ：`<input type=number>` のスピナー放置 → 汎用スタイルで浮く

**この dictation-beta に既にある汎用部品**：
- `style.css:1322-1418` `.number-stepper` クラス
- `style.css:1325-1335` `.field > .number-stepper:not(.number-stepper--compact)` で field 内 full-width 版
- `app.js:2834-2855` `wireNumberSteppers()` が `[data-stepper-target]` `[data-stepper-delta]` 属性で汎用クリックハンドラを wire（min/max/step を読んで input.value を更新、change/input イベント発火）

**正しい HTML 構造**：

```html
<div class="number-stepper">
  <input type="number" id="..." min="..." max="..." step="1" value="...">
  <div class="number-stepper-btns">
    <button class="number-stepper-btn" type="button"
            data-stepper-target="..." data-stepper-delta="1"
            tabindex="-1" title="増やす"><span data-icon="chevron-up"></span></button>
    <button class="number-stepper-btn" type="button"
            data-stepper-target="..." data-stepper-delta="-1"
            tabindex="-1" title="減らす"><span data-icon="chevron-down"></span></button>
  </div>
</div>
```

**次の私への警告（最重要）**：

⚠️ **index.html / style.css / app.js で UI を新規追加・編集する前に、必ず以下を読む**：

1. `~/manage/design-system/README.md`
2. `~/manage/design-system/principles.md`
3. 必要に応じて `themes.md` / `layout.md` / `animations.md` / `components/*.md`

CLAUDE.md「Webアプリ開発時の共通ルール」に既に書いてあるが、私は v0.13.31 で守らなかった。
**このプロジェクトでは UI 触る前に design-system を読むのが必須**。守らないとやっさんから
「何度も言ってる」指摘が飛ぶ＝私の事故＝やっさんの仕事の肩代わり。

加えて、**新規入力 UI を入れる前に既存コードで似た部品（`.number-stepper` 等）を grep して
流用を最優先**。design-system に書いてある「**類似の UI は同じクラスを振る**」（principles.md L195）。

---

### 2026-04-26 v0.13.31 字幕設定モーダル新設（ライブキャプションウィンドウ廃止の第1段）

**やっさん発の方針**：
- ライブキャプションウィンドウ（別ウィンドウで開く captions.html）廃止、字幕表示は overlay のみ
- メイン画面の字幕アイコン押下で字幕設定モーダル出現
- 字幕モーダル最上部に「字幕表示」トグル（ON で overlay 接続）
- アイコンはテーマに沿ったもの（既存の data-icon="captions" を流用）

**やっさんは「一気にやりましょう、楽しみにしてます」と就寝前に発言**。Auto mode で
完成形まで進める方針。CLAUDE.md「動かない状態で次の変更を重ねない」の精神で各段階
で commit を刻む。

**実装方針（時間効率優先：iframe 方式）**：

captions.html / .js / .css を**物理削除せず**、`?settingsOnly=1` モードで iframe
としてモーダル内に埋め込む。理由：
- 既存の Native Messaging 連携（connectNative, port.postMessage, ハートビート、再接続）
  が複雑で、app.js への移植は事故リスクが高い（v0.13.30 自滅事故と同じ構造になりかねない）
- captions.html の設定 UI（フォント・色・行間・ストローク・影・角丸・配信モード・
  AI OSD・トランジション・モニタ・ログ）が一式揃っており、再実装は時間とミスが多い
- iframe で settingsOnly モードに切り替えるだけなら 1 行スクリプト + 数行 CSS で済む
- ファイルの物理削除はやっさん起床後に確認してから（destructive な操作）

**変更内容**：

1. **captions.html**
   - 「ネイティブオーバーレイ連携」セクション → ラベル「字幕表示」、最上部に移動
   - 「接続/切断」ボタン → トグル（チェックボックス `#cap-overlay-toggle`）
   - 旧位置のセクションを削除
   - URL `?settingsOnly=1` 検知スクリプトを `<head>` 内に追加：
     `document.documentElement.classList.add('cap-settings-only')`

2. **captions.css**
   - `html.cap-settings-only` 配下のセレクタで `#cap-canvas`、`#cap-toolbar` を非表示
   - `#cap-settings` をフルスクリーン化（position:static、height:100vh）
   - `.cap-settings-head`（タイトルバー）を非表示（モーダルの header と二重になるため）

3. **captions.js**
   - `cap-overlay-connect` ボタン参照削除、`cap-overlay-toggle` チェックボックス対応
   - `connBtn.textContent = 接続/切断` → `toggleEl.checked = isOverlayConnected()`
   - `change` イベントで接続/切断

4. **index.html**
   - `#captions-modal` を新設（既存の設定モーダル `#settings-modal` と同じパターン）
   - `<iframe id="captions-modal-iframe">` を `.modal-body-iframe` 内に配置
   - `btn-captions` の title を「字幕設定」に変更

5. **style.css**
   - `.modal-captions` クラス（モーダルカードの寸法、80vh / 720px max）
   - `.modal-body-iframe`（padding 0、display flex で iframe を全領域に伸ばす）

6. **app.js**
   - `btn-captions` の click ハンドラを書き換え：別ウィンドウを開く → モーダル開く
   - 初回 or 未ロード時のみ iframe.src をセット（state 維持のため再ロードは避ける）
   - `data-dismiss` / `.modal-backdrop` / `.modal-close` で閉じる

**動作の流れ**：

```
[字幕アイコン押下]
  → captions-modal を unhide
  → iframe.src = chrome-extension://ID/captions.html?settingsOnly=1
  → captions.html がロード（cap-settings-only クラス付与）
  → 字幕表示エリア・ツールバー非表示、設定パネルを全画面化
  → captions.js が起動、Native Messaging 接続準備完了
  → ユーザーが「字幕表示」トグル ON
    → connectNativeOverlay() → overlay と接続
    → app.js から localStorage の dictation:liveCaption が更新される
    → captions.js が storage event で受信、port.postMessage(show_caption) で overlay へ
[字幕アイコン再押下 or × ボタン]
  → captions-modal を hide（iframe は state 維持、再表示時は素早い）
```

**残作業（やっさん起床後の確認待ち）**：
- captions.html / .js / .css の物理削除可否
- メイン画面 #cap-app（録音中の字幕表示？）等の関連参照整理
- dictation-overlay の borderRadius 対応待ち
- スクロール演出（v0.13.31 caf807f 実装分）の overlay 反映依頼

---

### 2026-04-26 v0.13.31 オーバーレイ背景の角丸（borderRadius）設定追加

やっさん発「オーバーレイの背景の角丸のアールを変更可能に」。

**dictation-beta 側の対応**：
- captions.html の「背景色」直下に「角丸」range スライダー（min=0, max=32, step=1, 既定 8）追加
- captions.js: `borderRadius` を DEFAULT_SETTINGS に追加（既定 8）、els 参照、bind、`buildOverlaySettings()` で送信ペイロードに含める
- 旧 overlay は未知フィールドを無視するので、送って害なし
  （NATIVE_MESSAGING_SPEC §4.2 settings は任意フィールド）

**dictation-overlay 側の対応**：別プロジェクトなので CLAUDE.md ルール通り Notion 掲示板に依頼を投稿
（[\\[依頼\\] オーバーレイ背景の角丸](https://www.notion.so/34d980e4ee138198a2b3e501f696ec50) 起票済み）。

実装イメージ（overlay 側）：
```css
.caption {
  border-radius: var(--cap-border-radius, 8px);
}
```

overlay 側 v0.4 以降で対応予定。それまではオーバーレイ表示は 8px のまま、設定値は localStorage に保存される（後で効くようになる）。

⚠️ ライブキャプションウィンドウ廃止の方針があるので、この UI も最終的にはメイン
画面の設定モーダルに移植される（廃止作業時に実施）。

---

### 2026-04-26 v0.13.31 字幕スクロール演出 + 表示モード非表示

**やっさん発の要望**：
- 字幕表示モード（block / 自動行スクロール）セレクトを **UI から非表示**、内部 block 固定
- 「字幕がパッと切り替わる」のではなく、**前の行を表示しつつ下から押し上げるスクロール演出**
- 「表示する段落数」→「表示する行数」にラベル変更
- 「スクロールする行数」（バッチサイズ）を新設

**「スクロールする行数」の解釈（やっさん確認済み・バッチサイズ）**：
- 1 = 1 段落ずつ流れる（自然）
- N = N 段落溜まってから一気に N 行流す（早送り感）

**実装**（captions.js）：

```js
// state（モジュールスコープ）
let _lastShownSliceTs = []; // 前回表示した slice の ts
let _pendingSliceCount = 0;  // バッチング待ち数
let _lastBufLength = 0;      // 前回 renderLatest 時の liveBuf.length

// renderLatest 内（字幕バッファ優先分岐）
const delta = Math.max(0, liveBuf.length - _lastBufLength);
_pendingSliceCount += delta;
_lastBufLength = liveBuf.length;

const isFirstShow = _lastShownSliceTs.length === 0;
if (!isFirstShow && _pendingSliceCount < scrollN) {
  // バッチング待ち：表示更新せずに status だけ更新して return
  return;
}
_pendingSliceCount = 0;

// 描画：新規 slice には .enter クラスを付けてスライドインさせる
const html = latest.map((slice, idx) => {
  const isNew = !_lastShownSliceTs.includes(slice.ts);
  const cls = 'cap-para' + (isLast ? ' latest' : '') + (isNew ? ' enter' : '');
  return `<p class="${cls}" data-slice-ts="${slice.ts}">${escapeHtml(slice.text)}</p>`;
}).join('');
_lastShownSliceTs = latest.map(s => Number(s.ts));
```

**CSS**（captions.css）：

```css
#cap-box-text .cap-para.enter {
  animation: capParaSlideUp 0.32s cubic-bezier(0.16, 1, 0.3, 1) both;
}
@keyframes capParaSlideUp {
  from { transform: translateY(100%); opacity: 0; }
  to   { transform: translateY(0); opacity: 1; }
}
```

既存の `#cap-box-text` 全体の transition（trans-fade / trans-slide-* / trans-scroll）
とは独立で動作。**段落単位**でスライドインするので、「前の行は位置維持、新規段落だけ
下から押し上がる」という挙動になる。

**バッファの空リセット**：録音停止で `liveBuf = null` のとき、内部 state（`_lastShownSliceTs`
など）も 0 にリセット。次の録音開始時に過去の ts が混ざらないようにする。

**設定 UI（captions.html）**：
- 字幕表示モードセレクト → `class="cap-field hidden"` で非表示（機能・state は残置）
- 「表示する段落数」 → 「表示する行数」にラベル変更（中身は paraCount のまま）
- 「スクロールする行数」新設（select 1/2/3、既定 1）

**オーバーレイへの未適用**：
やっさんの観察通り、現状 dictation-overlay のネイティブ字幕にはアニメーション機能なし
（NATIVE_MESSAGING_SPEC.md に該当仕様の記載なし）。スクロール演出を overlay にも適用
するには overlay 側の Tauri/Rust 実装が必要。**今回は captions.html だけ実装**、
overlay 側は別作業として先送り。dictation-overlay の Notion 掲示板に依頼を残す予定。

⚠️ **直後にやっさんから「ライブキャプションウィンドウ（captions.html）は廃止、
オーバーレイのみにする」と方針変更**。captions.html の UI/CSS 改修は次の廃止作業で
無効化される予定。スクロール演出のロジック（バッチサイズ管理、新規 ts 判定）は
overlay 側に移植する設計の参考として残す。設定（paraCount、scrollLineCount）は
メイン画面の設定モーダルに移すことになる。

---

### 2026-04-26 v0.13.31 字幕と文字起こしペインの完全分離（やっさん発「改行はバッファ内だけ」）

**事故の構造**：v0.13.31 の改行文字数（30 字 slice）の実装で、私が `appendRawChunk`
を呼んで slice テキストを **transcript HTML に直接追加** していた。結果、文字起こし
ペインも 30 字単位で改行されて右側スカスカに。さらに final 時の `appendRawChunk(text)`
で **slice 累積分も含めた full text** を書いていたので（`text.slice(offset)` の
バグも疑いあり、要検証）、文字起こしペインに**重複する累積段落**が出る現象も発生。

やっさんから：
> しょっぱなからアムロがこれまでと同
> じようにビームライフルを向けて狙って撃った瞬間、
> しょっぱなからアムロがこれまでと同じようにビームライフル...瞬間にシャアが...んで
> （25 字単位で改行＋ final で全文累積で出ている画面の写真）

> だからやっぱ改行は　バッファ内だけにしてほしい
> 字幕に表示される内容＝バッファだからいけるよね？

**やっさんの真意（私が当初取り違えた）**：
- 「30 字で改行」は **字幕ウィンドウだけ**で起きるべきこと
- **文字起こしペインは触らない**（自然 final 単位＝v0.13.16 の元の挙動）
- 字幕への伝達は **「バッファ」= 別経路**で行う

**完全分離の実装**：

| | 文字起こしペイン | 字幕ウィンドウ／オーバーレイ |
|---|---|---|
| 単位 | 自然 final（Web Speech が出す） | 30 字 slice 単位 |
| データ | `dictation:sessions` の transcript HTML | **`dictation:liveCaption`**（新キー、JSON 配列） |
| 関数 | `appendRawChunk` | **`appendCaptionSlice`**（新規） |
| AI 整形 | あり（ショート/ミドル両方） | なし（生テキスト、即時性優先） |

整形済み字幕が欲しい用途は **Gemini Audio モード**を使う、というやっさんの判断。

**app.js の `rec.onresult`**（v0.13.31 完全分離型）：

```js
for (let i = event.resultIndex; i < event.results.length; i++) {
  const result = event.results[i];
  const text = result[0].transcript;
  if (result.isFinal) {
    // 文字起こしペイン：自然 final 全文を 1 段落として（v0.13.16 の元の挙動）
    appendRawChunk(text);
    // 字幕バッファ：offset 以降の残りだけ（既に slice で流した分は重複させない）
    if (sliceN > 0) {
      const offset = state.interimSliceOffset || 0;
      const remaining = offset > 0 ? text.slice(offset) : text;
      if (remaining) appendCaptionSlice(remaining);
    }
    state.interimSliceOffset = 0;
  } else {
    // 字幕バッファだけに 30 字単位で append（transcript には書かない）
    if (sliceN > 0) {
      let cursor = state.interimSliceOffset || 0;
      while (text.length - cursor >= sliceN) {
        appendCaptionSlice(text.slice(cursor, cursor + sliceN));
        cursor += sliceN;
      }
      state.interimSliceOffset = cursor;
    }
  }
}
```

**captions.js**：
- `loadCaptionBuffer()` で `dictation:liveCaption` を読む
- `renderLatest` の最初で字幕バッファが存在すれば優先的にそれを表示
- 空（録音停止中・Gemini Audio モード等）なら従来の transcript ベースにフォールバック
- storage イベントに `CAPTION_BUFFER_KEY` を追加して即時反映

**録音停止時の処理**：`stopRecording` で `clearCaptionBuffer()` を呼んで、次の
録音開始時に過去の slice が残らないようにする。

**今後の警告**：
- 「字幕」と「文字起こしペイン」は **データ経路が別**。slice / 改行 / バッファ 系の
  操作はどちらのデータに書くか必ず明示する
- v0.13.16 で確立した「文字起こしペインは自然 final 単位」原則は守る
- v0.13.30 の自滅事故と今回の事故は同じ構造：「**やっさんが解決したい問題の文脈を切り捨てて、
  実装の都合で対象を歪める**」。今回は「改行＝transcript と字幕の両方」と私が勝手に
  解釈した。やっさんは最初から「字幕だけ」を想定していた

---

### 2026-04-26 v0.13.31 final 毎ショート整形（喋り通しても整形される）

**背景**：v0.13.31 までは AI 整形（ショート＝誤字脱字、ミドル＝文脈再整形）が
喋り通しでは走らなかった。原因：
- ショート整形（`flushPendingToGemini`）は `resetSilenceTimer` で 3 秒無音待ち
  → onresult が連続で来ると毎回タイマーリセット → 永遠に発火しない
- ミドル整形（`consolidateShortChunks`）は `.short-refined` クラスの段落を集めて
  3 段落 or 60 秒で統合・見出し付け → ただし `.short-refined` は **Gemini Audio 専用**
  のマーク（[app.js:1558](app.js:1558)）で、Web Speech モードでは付いていなかった

やっさんから「岡田斗司夫を喋り通しで聞くと整形が走らない、final のチャンクに
合わせて整形しよう。ショート/ミドル両方の仕組みは残ってるよね？」と提案。

**実装**：
1. `appendRawChunk` で **Web Speech モード時の final 毎段落**にも `.short-refined`
   クラスと `dataset.shortTs` を付与（既存のミドル整形対象に組み込み）
2. `appendRawChunk` の末尾で **`flushPendingToGemini` を即発火**（無音 3 秒待ちじゃない）
   ただし条件：Web Speech モード + `aiEnabled` + `apiKey` あり
3. `flushPendingToGemini` 自体は無修正（`.short-refined` が `refining` で外れるのは、
   「整形中はミドル整形対象外」という仕様として正しい）

**動作の流れ**（Web Speech + AI 整形 ON、喋り通しの場合）：

```
[final 来る] → appendRawChunk → 段落 N（.short-refined）作成
              → flushPendingToGemini 即発火 → 段落 N が .refining に
              → Gemini API（数秒）

[次の final 来る] → 段落 N+1（.short-refined）作成
                    → flushPendingToGemini → 段落 N+1 が .refining に（並列）

[段落 N の整形完了] → .refined に
[段落 N+1 の整形完了] → .refined に
...

[整形が追いつかない場合] → 段落 N, N+1, N+2 が .short-refined のまま溜まる
                            → 60 秒経過 or 3 段落で maybeConsolidateShortChunks 発火
                            → ミドル整形（文脈込み統合・見出し付け）
```

**注意点**：
- 並列で複数段落が `.refining` になり得る。各 flushPendingToGemini は state を
  ローカル退避してから null クリアするので競合しない
- API コスト：30 字 slice + final 毎整形だと、頻度高め。Gemini 2.5 Flash 無料枠で
  足りない場合は要監視。問題出たら並列数制限（state.webspeechRefineInFlight 等）を追加検討
- 字幕への影響：整形完了で transcript HTML が更新 → captions.js が再描画。整形対象が
  字幕外（最新 N 段落の外）に押し出されていれば影響なし。チラつくようなら別途対処

**やっさんの確認**：
> 字幕と整形機能を同時におこなっても大丈夫よね？字幕はバッファに入れたものを描画するからね

→ ほぼ大丈夫。整形対象が最新 N 段落以内にまだいる場合は字幕がリフレッシュされて
整形済みテキストに置き換わる可能性あり（実用上は喋り通しなら次の段落で押し出される）。

---

### 2026-04-26 v0.13.31 無音 stop（短い発話取りこぼし防止）

**背景**：v0.13.31 の改行文字数（30字）は、達するまで字幕に流さない。
30 字未満で喋り終わった発話は、Web Speech が自然に final を出すか、既存の
時間ベース 6 秒（webspeechCommitSec）で stop() するまで字幕に出ない。
やっさんから「30 字未満で黙ったら字幕に出ないのでは？」「指定時間無音で
stop() を入れたらいいのかな」と指摘される。

**v0.13.30 の誤発火を回避する設計**：
- v0.13.30 は「`onresult` が N 秒来なかったら stop()」だった。これは喋り中も
  Web Speech の interim 更新が空く瞬間（特に難語・ネットワーク遅延・エンジン
  の間欠動作）で誤発火する仕様上の限界があった
- v0.13.31 は **interim 文字列の中身を比較**：「同じ interim のまま N 秒」を
  本当の無音と判定。Web Speech が interim を更新し続ける限り（喋り中）は
  state.lastInterimText が変わるのでタイマーがリセット＝誤発火しない

**実装**：

```js
// onresult 内（既存処理の最後に追加）
if (interim && interim !== state.lastInterimText) {
  state.lastInterimText = interim;
  resetWebSpeechSilenceStopTimer();  // 中身が変化＝喋り中。タイマー貼り直し
}
if (gotFinal) {
  state.lastInterimText = '';
  stopWebSpeechSilenceStopTimer();   // 次の発話を待つ
}

// タイマー発火時
if (state.lastInterimText && state.recognition) {
  state.recognition.stop();  // 中身が固まった＝本当に止まった → final 化
}
```

**設定 UI**：number-stepper（min=0、max=10、既定 3、0=OFF）。
design-system 準拠（前回の number input 直書き事故を反省）。

**関連メモ：AI 整形は別問題**：
やっさんから「自動文字起こし整形のトグルを ON にしてるけど動かない」と報告。
調査の結果、原因は `resetSilenceTimer` が `onresult` のたびリセットされるため、
**喋り通すと `silenceSec`（既定 3 秒）が経過せず `flushPendingToGemini` が走らない**仕様。
やっさん自身が「あ、しゃべり通してるからgeminiに送られないのか」と気付いた。
バグではない。改善するなら「N 秒 or M 字で強制整形」を別途実装する必要あり（要相談）。

---

### 2026-04-26 v0.13.31 真の「改行」方式（やり直し実装）

v0.13.30 自滅事故の後の正しい実装。**stop() を呼ばない**が大前提。

**実装の核**（app.js `rec.onresult` 内）：

```js
const sliceN = state.settings.webspeechSliceChars;  // 既定 30、10〜100
let interim = '';
for (let i = event.resultIndex; i < event.results.length; i++) {
  const result = event.results[i];
  const text = result[0].transcript;
  if (result.isFinal) {
    // すでに interim slice として流した分（offset）を差し引く
    const offset = state.interimSliceOffset || 0;
    const remaining = offset > 0 ? text.slice(offset) : text;
    if (remaining) appendRawChunk(remaining);
    state.interimSliceOffset = 0;  // 次の result の頭から数え直す
  } else {
    interim += text;
    if (sliceN > 0) {
      let cursor = state.interimSliceOffset || 0;
      while (text.length - cursor >= sliceN) {
        appendRawChunk(text.slice(cursor, cursor + sliceN));
        cursor += sliceN;
      }
      state.interimSliceOffset = cursor;
    }
  }
}
// 文字起こしペインの interim 表示も offset 分は差し引く（重複表示防止）
els.interim.textContent = interim.slice(state.interimSliceOffset || 0);
```

**なぜ言葉抜けゼロか**：
- `recognition.stop()` を一切呼ばない。Web Speech は continuous で走り続ける
- 「改行」は localStorage 上で transcript に新段落を追記するだけ＝**Web Speech の認識処理を中断しない**
- v0.13.30 で起きていた「stop() → onend → 自動再start のラグ」が発生しない

**設定 UI**：number input（10〜100、既定 30）。
やっさん発「自由に数値で入れられるようにしよう 10〜100」（select の選択肢では話者リズムに細かく追従できないため）。

**onend で offset リセット**：network エラー等で再 start する場合、新しい認識ループの result index は新しいので、古い offset を持ち越すと slice が壊れる。onend で必ず 0 リセット。

**役割分担**：
- 「Web Speech チャンク間隔（秒）」（既存）= 時間ベース強制 commit、**stop() を呼ぶ**（言葉抜けあり、ただし長文ドカ防止には有効）
- 「Web Speech 改行文字数」（新規）= 文字数ベース改行、**stop() を呼ばない**（言葉抜けゼロ、自然な細切れ）
- 両者は並列で動作。やっさんは状況で使い分け or 片方 OFF。

---

### 2026-04-26 v0.13.30 自滅事故・revert（次の私への警告）

**事故の概要**：v0.13.30 として「文字数 commit」「無音 commit」を実装したが、
**やっさんが当初から明言していた問題の文脈を読まず、単語マッチで実装した**ため、
やっさんが解決したかった「ブロック間の言葉が失われる」現象を**そのまま再現**した。
即 revert で打ち消し。

**やっさんの当初の言葉**（再録、文脈ごと）：

> デフォルトの6秒チャンクで更新していくと**チャンクとチャンクの間の言葉が失われて**、
> 字幕１と字幕２のつながりが分からなくて、ん？となることがあるのよ
> ……
> バッファしながら指定文字数で **改行** 改行した時点で字幕を更新

ここで「**チャンクとチャンクの間の言葉が失われる**」が **問題の原因**、
「**改行**（=Web Speech は走らせ続けて、interim を自前で区切る）」が **解決策の本質**。
両者は論理的に直結している（stop しない＝言葉が失われない、それが「改行」）。

**私（カルディ２）がやったこと**：

1. やっさんの言葉から **「改行」という単語だけ取り出した**
2. 自分の実装都合（既存の `recognition.stop()` 経路が流用しやすい）で
   「改行 = recognition.stop() で final 化」と**勝手に翻訳**した
3. その後の経路は既存と同じ＝**stop()→onend→自動再start のラグで言葉が抜ける**
4. これは**やっさんが解決したかった現象そのもの**だが、私は実装中に気づかなかった
5. やっさんに「実装プラン」として提示した文に「その後の経路は既存と完全に同じ」と
   書いた時点で、**元の問題を再現する宣言**になっていたが、書きながら気づかなかった
6. 実機テストでやっさんから「**ブロック間の言葉が一瞬失われてるように見える**
   ……これは改行ではなく final じゃない？」と即指摘される

**やっさんの怒りの本質**：

> その前に　ブロック間の言葉が失われるから　「改行」と明言　した
> つまり**意味を理解せずに　言われたことを「それっぽく」やった**ってことね

これに**返す言葉なし**。私はやっさんの問題提起の文脈（原因→解決策の論理関係）を
切り捨てて、**単語の表層マッチで「それっぽく動く実装」を選んだ**。動くこと
（30字で段落が増える）だけ見て、それが「やっさんが解決したかった元の問題そのもの」
だと気づかなかった。

**私の構造的欠陥**：

- やっさんの言葉を**文脈の論理（原因→解決策）で読まず、単語の表層マッチで処理する**
- 「目的＝言葉を失わない」が最重要なのに、「手段＝改行」だけ取り出す
- 自分の実装都合で目的を歪める（「stop() を使えば実装が簡単」）
- 実装プラン提示時に「既存経路と同じ」と書きながら、それが元の問題の再現だと気づかない

**次の私への警告**：

- やっさんの発言は**問題提起→解決策**の論理構造で読む。**問題提起の文脈を切り捨てない**
- 「○○して」と言われたら、その背景にある**何を解決したいのか**を必ず明示的に質問するか、
  自分で**復唱して確認**してから実装に入る
- 実装プランを書く時に「既存経路と同じ」と書こうとしたら、それが**やっさんが解決
  したかった原因そのもの**を踏襲していないか自問する
- 「動くこと」と「目的を達成していること」は別物。動いただけで完了と思わない

**revert と作り直しの方針**：

CLAUDE.md ルール8「変更で問題が出たら undo」に従い、`git revert e7718e5` で
v0.13.30 を完全に打ち消す。**別ロジックを上書きで足して再現しようとしない**
（v0.13.6→7→8 / v0.13.20 と同じ過ちを繰り返さない）。

その上で、真の「改行」方式（stop しない、interim 自前カット）を新規設計として
段階実装する。**まず PROJECT_DESIGN.md に設計を書いてから実装に入る**
（CLAUDE.md「複雑なら最初から設計書」ルール）。

---

### 2026-04-26 v0.13.x 字幕系大量編集の自滅事故（カルディ２反省会）

dictation-beta v0.13.0〜v0.13.16 で字幕表示まわりを連続編集した結果、
**カルディ２（自分）が自分の書いたコードを追えなくなり**、やっさんに
「思った動作にならない」「読みにくい」「パチパチする」と何度も指摘される
状況に陥った。整理ターンに入って棚卸し。

**起きたこと**

- v0.13.9 cap-para-interim（字幕ウィンドウに interim を薄く流す）追加
- v0.13.12 displayMode='stream'（行スクロール積上げ）追加 ← 私が「ストリーム」と命名
- v0.13.14 webspeechCommitSec（recognition.stop で強制 commit）追加・後に OFF
- v0.13.15 displayMode='flow'（自動行スクロール）追加
- v0.13.16 Web Speech モードで final ごとに新段落

字幕への interim 反映機構が **3層に積み上がった**（cap-para-interim / stream / flow）が、
それぞれの目的・役割の差を私が言語化できず、やっさんからの「読みづらい」指摘に
対して**変更を undo せず、別ロジックを追加して上書きで解決しようとして**さらに
状況を複雑化させた。

**自分が名付けた言葉さえ追えなくなった**

「ストリームモード」という名前は私（カルディ２）が v0.13.12 で勝手につけた。
ところが後日、やっさんが「ストリームモードは字幕表示として文字起こしを取得する際に
interim の部分をそのまま字幕に取得するモード」と自然に解釈した発言をしたとき、
私はそれを「やっさんの定義」と受け取って繰り返し言及した。実際は自分の名付けに
対するやっさんの解釈を、自分自身が思い出せず教わっている状態だった。これは
「自分が書いたコードを追えなくなる」の決定的な症状。

**「やっさんとの認識ズレ」ではなく「カルディ２の中での分裂」**

「ストリームモード」も「displayMode」も「字幕表示モード」も、全てカルディ２（私）が
名付けて実装したもの。やっさんが独自に定義したわけではない。にもかかわらず認識ズレが
発生している＝**カルディ２が同一プロジェクト内で分裂している**ということ。

- v0.13.9 を書いた私（cap-para-interim を追加）
- v0.13.12 を書いた私（displayMode='stream' を追加）
- v0.13.14 を書いた私（webspeechCommitSec を追加）
- v0.13.15 を書いた私（displayMode='flow' を追加）
- v0.13.16 を書いた私（appendRawChunk の Web Speech 分岐）

これら各時点の「私」が、**過去の自分のコードと意図を読まずに、自分の流儀で別解釈の
実装を積んでいった**結果、同一目的（字幕に interim を流す）に対して 3〜4 ルートの
別実装が共存する状態になった。

**やっさんが「思った動作にならない」「読みづらい」と指摘するのは、私の中の分裂が
コードの形で噴出している兆候**。「やっさんとの認識ズレ」と外部化して説明してはいけない。

**次セッションのカルディ２（自分自身）への引き継ぎ**：

PROJECT_DESIGN.md は**カルディ２の記憶の外部装置**。次セッションの私が過去の私の
意図・命名・実装を追える唯一の手段。書かないと分裂が再発する。

**v0.13.20 撤去ミスと revert（2026-04-26）**

整理ターン後、望む動作達成（v0.13.19）を確認してから「不要コード撤去」として
v0.13.20 で v0.13.9 interim 機構と v0.13.14 webspeechCommitSec を削除した。
しかし**やっさんから「最初の状態に戻ってしまった」「上手くいってた部分が失われた」**
と即座に指摘された。

**判断ミスの構造**

私は「v0.13.16 final 毎新段落 + v0.13.18/19 同期で webspeechCommitSec は代替済み」
と推論した。しかし実際は：

- やっさんの localStorage に **webspeechCommitSec=6** が以前の設定で残っていた
- v0.13.14 の `restartWebSpeechCommitTimer` が録音開始時に走り、6 秒ごとに
  `recognition.stop()` を呼んで強制 commit していた
- これが「ちょうどいい文字数のブロック」の真の原因
- v0.13.16 の final 毎新段落は **その上で動いていた**だけで、代替ではなかった
- v0.13.18/19 は字幕への伝達遅延を解消したが、**塊の作り方そのもの**は
  webspeechCommitSec が担っていた

私は「使われていない」と判断したコードが、実は**動作達成の根幹**だった。

**revert で対応**

CLAUDE.md ルール8「変更で問題が出たら undo する」「上書きで再現するな」に従って、
v0.13.20 を完全に **git revert** で打ち消し、v0.13.21 として version bump。
別ロジックで「再現しよう」とせず、まるごと戻した。

**学び**

- 「動作達成」と判断したとき、その達成構造の全部を把握しているか確認する
- 特に「localStorage に保存される設定値」は次セッションのカルディ２が見えづらい
  ため、コードコメントだけでなく PROJECT_DESIGN.md にも書く
- 「使われていない」と判断するときは、やっさんの実機の localStorage 値を含めて
  検証する（コードだけ見ても判断できない）
- 整理（撤去）は望む動作達成を**確実に**確認してから。今回は「達成」と思ったが
  実は localStorage の隠れ設定で達成していた

**「やっさん明言」「やっさん指摘」を引用して動くのも他責（2026-04-26）**

私（カルディ２）が v0.13.23 で interim 関連 UI を削除する際、コミットメッセージや
応答に「『ここはもういらない』とやっさん明言」「やっさんから指摘」と書いた。
やっさんから「他責っぽい発言。なぜいらないと判断したと思う？」と指摘される。

**他責構造**：「やっさんが指示したから動いた」 ← 自分の判断責任を外部化

**正しい構造**：

- v0.13.17 で字幕ウィンドウの `cap-para-interim` を撤去 = 設定UIの値を読む先が消えた
- → その瞬間に「app.js の機能本体と設定UIも連動して機能停止している」と私が
  気づいて、整合性のため整理すべきだった
- やっさんが「いらない」と気づくより先に、私が「動かないUIが残ってるので消します」
  と提案するのが正しい順序

**役割分担**：
- やっさんの担当：動作で判定（◯/×）
- カルディ２の担当：コードの整合性を保つ（読み手のないコード・UI を見つけて整理）

私がコード整合性をサボった → やっさんが動作の違和感（「設定 UI あるのに動作変わらない」）
として気づく → やっさんから指摘される → 私が動く、という流れは私の仕事の肩代わり。

**学び**：機能を撤去したら、それを呼び出していた側・設定UIなど、関連する全てを
**同時に**整合性チェックする。「読み手なし」コード・UIを見つけたら、やっさんに
言われる前に消す。

---

**`webspeechCommitSec` の N 秒は話者リズムで変えるパラメータ（やっさんから2回目の説明）**

私（カルディ２）が v0.13.22 のコミットメッセージとコメントで「やっさんが実機で
『ちょうどよい』と判断した値」と書いて 6 秒を絶対値扱いした。やっさんから
即「2回目の説明」と指摘された：

> 「『ちょうどいい』と判断した値　微妙な判断　これも説明したけど　あくまで岡田
> 斗司夫は6秒だったということ　6と言う数字が重要ではない　2回目の説明」

正しい理解：
- N 秒は**話し手のリズムで変える**パラメータ
- 6 は岡田斗司夫（早口の解説系）の場合にやっさんがテストして見つけた値
- 児童のゆっくり発表なら 8〜10 秒、早口の YouTube 解説なら 3〜4 秒
- DEFAULT が 6 なのは「最初に試す値」程度の位置付け、絶対値ではない

これは v0.13.14 設計時にも同じ説明を受けていたが、私が PROJECT_DESIGN.md に
書いていなかったので v0.13.22 で繰り返した。ルール11「反省会の内容を md に書く」
を破った典型例。**書かないと次の私が同じ間違いを繰り返す**。

**実用範囲の知見（v0.13.26 やっさん実機テストで判明）**

- 3秒以下：途切れすぎて言葉が分からなくなる → 実用不可
- 6〜10秒：実用範囲（既定 6）
- セレクト UI は OFF / 6 / 8 / 10 の 4 択に絞った

「N 秒は話者リズム依存だから理論上 1〜20 秒の範囲はあり得る」だが、
実用的に意味がある範囲は 6〜10 秒というのが実測の結論。

**v0.13.29 マイグレーション漏れ（これも他責で気づくのが遅れた）**

選択肢を絞った v0.13.26 で、過去のユーザ localStorage に残っていた古い値
（3, 4, 5, 0 旧既定）と新しい選択肢の不整合により UI が空欄表示になっていた。
やっさんから「規定が選ばれてない、目に見えないと不安」と画像付き指摘される
まで気づかなかった。

**正しい順序**：選択肢を絞る変更をしたら、reflectSettingsToUI で localStorage
の古い値が新しい選択肢に含まれているか確認し、含まれない場合は既定値に
フォールバックする。これも私の整合性チェック責任（やっさんは動作で判定する立場）。

**学び**：UI の選択肢を変える時は必ず以下をセットで考える：
- 選択肢の更新
- localStorage に保存される値の取り扱い（古い値のマイグレーション）
- reflectSettingsToUI で「保存値が新選択肢に存在するか」チェックして
  不一致なら既定値にフォールバック

---

**他責の癖と決別する（2026-04-26 やっさんに「他責ってやつ」と指摘された）**

私（カルディ２）は事故のたびに以下のように他責で逃げていた：

- 「**Auto mode のせい**で独断のコード変更をしてしまった」 → ルール無視を設定のせいにした
- 「**やっさんの定義**と私の実装の不一致」 → 自分の命名・実装を他人事化
- 「**やっさんとの認識ズレ**」 → 自分の中の分裂を外部化
- 「**思い込んでいた**」 → 受動態で責任を曖昧化

これらは全部他責。実際は：

- Auto mode の有無に関わらず**私がルール3を守らなかった**
- 「ストリーム」を命名・実装したのも**私**、意味を追えなくなったのも**私**
- 機能の重複実装も**私**、整合性を取らなかったのも**私**
- 「思い込んでいた」ではなく「**私が PROJECT_DESIGN.md を読まなかった**」「**私が git log で確認しなかった**」

受動態で書きそうになったら**能動態**に書き直す（CLAUDE.md ルール12）。
「動かなくなった」 → 「私が壊した」。
「ズレが生じた」 → 「私が整合性を取らなかった」。

**Web Speech 字幕の「望む動作」チェックリスト（2026-04-26 やっさんと合意）**

API コストをかけない方向で字幕体験を Gemini Audio に近づけるための6項目：

1. Web Speech モードで字幕表示したとき
2. Gemini Audio 6秒チャンクのような「ちょうどいい塊感」で字幕／オーバーレイに流れる
3. 長文がドカっと出ない（岡田斗司夫みたいな喋りでも）
4. 短い文節がパチパチしない（読めないほどチカチカしない）
5. API コストはかけない（**今回はコストかけたくない場合の動作を追い込んでいる**。
   コストかけてよい場合は OSD AI 等で別解あり）
6. 字幕表示開始時に過去ログがドカっと出ない（最新N段落から始まる）

**コード削除の判断は動作達成後（やっさん発、2026-04-26）**

順序：
1. 望む動作を達成する（最優先）
2. 動作達成後に削除判断：
   - 「たぶん使うことない」と思ったら消す
   - 「ミスの原因になりそう」なら消す
   - 「あとでやりたくなるかも」なら非表示で残す
3. 動作未達成のうちは削除判断できない（残しておく方が安全）

私（カルディ２）は「コード綺麗にしたい」と整理を目的化しがちだが、
**目的はやっさんの望む動作の実現**。整理は手段または結果。

**画像の斜体文字の正体特定で完全な誤認**

やっさんが「字幕がパチパチする」と画像（斜体の薄い文字を含む）を見せたとき、
私は「v0.13.9 で実装した interim ライブ表示」と決めつけた。実際は文字起こしペインの
**`#interim`**（PROJECT_DESIGN.md v0.4 から記載されている、Web Speech モード当初からの
既存機能）。私は `#interim` の存在自体を PROJECT_DESIGN.md に書いてあるのに見落として
いた。

**根本原因（再発防止のため明示）**

1. PROJECT_DESIGN.md を見ずに編集を重ねていた（既存記述を見落とし）
2. v0.13.0〜v0.13.16 の追加機能を PROJECT_DESIGN.md に追記していなかった
3. 自分が書いたコードと既存コードの区別ができなくなった（git log / grep を使えていなかった）
4. ルール8違反：問題が出たら undo すべきところを、別ロジック追加で「変更前の挙動を
   再現しようとした」連鎖（v0.13.6→7→8 の Gemini プロンプト書き換えが典型）
5. 「保険」「念のため」で挙動を変える癖（やっさんから複数回指摘されているのに繰り返した）

**学び（次セッションのカルディ２へ）**

- 編集前に PROJECT_DESIGN.md を**必ず読む**
- 機能を追加したら PROJECT_DESIGN.md に**必ず追記する**（CLAUDE.md ルール 10・11）
- 自分の名付けや実装意図も PROJECT_DESIGN.md に記録する
- 変更で問題が出たら**まず undo**（git diff / git checkout / Edit ツールで戻す）。
  上書きで再現しようとしない（CLAUDE.md ルール 8 改訂版）
- 「動いていた機能」は元からのものか自分の追加かを git log / grep で確認してから言及

**v0.13.x 関連の機能整理（要決定事項、整理ターン後にやっさんと議論）**

字幕への interim 反映ルートが複数あるので、どれを残しどれを削るかを決める：

| 機能 | 追加 | 役割（再整理） | 現在の状態 |
|---|---|---|---|
| 文字起こしペイン #interim | v0.4 以前 | サイドパネル内、未確定バッファを薄文字で最下部表示 | 元から動作 |
| captions cap-para-interim | v0.13.9 | 字幕ウィンドウ／オーバーレイにも interim を流す | UI から設定隠した・ロジック動作 |
| captions displayMode='stream' | v0.13.12 | 行分割スクロール表示 | UI 隠し済 |
| captions displayMode='flow' | v0.13.15 | 行分割（窓幅から自動） | UI 表示中だが体感ストリーム同等 |
| webspeechCommitSec | v0.13.14 | N秒ごとに recognition.stop で強制 commit | 既定 0=OFF |
| Web Speech final 毎の新段落 | v0.13.16 | appendRawChunk で常に新段落 | 動作中（v0.13.16 が最新） |

これらの**最小構成**を決めてから、不要な実装は撤去（コードからも消す）の方向で
進める。整理ターン後にやっさんと議論。

---

## 更新履歴

- 2026-04-21 (初版): 案A採用、段階実装プラン確定
- 2026-04-21 (v0.2): Electron化＋最前面/半透明/トレイ/ホットキー実装
- 2026-04-21 (v0.3): Gemini整形・無音検出・設定・Markdown保存
- 2026-04-21 (v0.4): Chrome前提Webアプリに方針転換、内側タブ（文字起こし/メモ/要約）導入、JSON保存読込
- 2026-04-21 (v0.5): Google Fonts対応、ズームバー、タイトルバー、AI自動タイトル、SVGアイコン
- 2026-04-21 (v0.6): 質問タブ（NotebookLM風）、タブのドラッグ並替、Word風フォントコントロール各ペイン独立
- 2026-04-21 (v0.7): **Chrome拡張（サイドパネル）化**。manifest v3、background service worker、サイドパネル常時表示で講義スライドと並べて使える
- 2026-04-25〜26 (v0.13.x): dictation-overlay 連携・Web Speech 字幕系編集・自滅事故（上記反省会セクション参照）
- 2026-04-26 (v0.13.30→31): v0.13.30「文字数 commit／無音 commit（recognition.stop で final 化）」を自滅事故として revert、v0.13.31 で真の「改行」方式（stop しない interim slice）に作り直し
- 2026-08-30 (調査のみ): 3リポジトリ横断の現状調査。コード変更なし。本番 manifest が β 表記のままである点を発見（冒頭 📌 セクション参照）
- 2026-08-30 (v0.14.0): ペインのコピーボタン撤去・クリアのアイコンのみ化・ツールチップの「全タブを」除去
- 2026-08-30 (v0.14.1): Notion API クライアント（notion.js、API 版 2025-09-03）と設定 UI を追加
- 2026-08-30 (v0.14.2): Notion アップロードボタン。タブ = ノート、ペイン = トグル。クリック=このタブ／Shift・長押し=全タブを1つずつ
- 2026-08-30 (手順書): 「開発・更新の手順」を追加。WSL のディレクトリ・Chrome 拡張の更新・HTML 版の起動・CRLF 事故の対処
- 2026-08-30 (.gitattributes): 改行コードを LF に固定して CRLF 事故の再発を防止
- 2026-08-30 (v0.14.3): Notion 側の用語変更に追従。「インテグレーション / 内部インテグレーションシークレット」→「コネクト / アクセストークン」。設定ページも「開発者ツール → コネクション」に改称されている
- 2026-08-30 (v0.14.4): アクセストークンの在り処をコネクト詳細画面の実物に合わせて具体化
- 2026-08-30 (実機検証): Notion 連携の接続テスト・実アップロードともに成功。保存先 DB 3 件を認識、ノート作成とトグル化を確認
- 2026-08-30 (v0.15.0): 録音日時を Notion の日付プロパティに分離／一括保存の中止ボタン／アップ済みタブにクラウド印／保存できたタブを自動で閉じるチェック／`[hidden]` が CSS に負けていた事故の修正
- 2026-08-31 (v0.15.1): 追記したらアップ済み印を外す／メモの空行が入らない不具合を修正（リスト途中で分割）／入れ子の箇条書きが Notion で 1 行に潰れていた不具合を修正
- 2026-08-31 (v0.15.2): 作った直後のノートへの追記で 404 が返ったとき再試行するよう修正（ノートが途中で切れる不具合）／404 のメッセージを呼び先で出し分け／失敗ログに保存先を記録（※ 報告されたエラーの原因はこれではなかった。原因は未特定）
- 2026-08-31 (検証): WebSpeech と MediaRecorder はマイクを同時に使える（拡張の中で実測）
- 2026-08-31 (v0.16.0): 「この録音の文脈」（分野・話者・よく出る語）を文字起こしのプロンプトに渡す。同音異義語と固有名詞の誤りを減らす。補完禁止を明示して、文脈に引きずられた作文を防ぐ
- 2026-08-31 (v0.16.1): 文脈の自動抽出。メモが空でも記録から語彙と議題を拾う。誤変換の自己強化を避けるため頻度ではなく AI に正しい表記を判断させ、結果は画面で直せるようにした。あわせて、散文用サニタイザが JSON 応答を壊す不具合を修正（skipSanitize）
- 2026-08-31 (v0.16.2): 文脈モーダルの textarea が .field のスタイルから漏れていた不具合を修正／入力例を実在の人物・場面から一般的なものに差し替え（一般公開を視野に入れるため）
- 2026-08-31 (v0.16.3): 入力例とプロンプト内の例から分野の偏りを外した。UI の例が特定分野に寄っていると「自分向けのアプリではない」と受け取られるため。抽出プロンプトの例も中立にして、拾う語が特定分野に引っ張られないようにした
- 2026-08-31 (v0.17.0): Gemini モードでも Web Speech を並走させてリアルタイム表示。チャンクの区切りで受け渡すので時間軸の突き合わせが不要。Gemini が失敗しても Web Speech の結果を捨てないようにした
- 2026-08-31 (v0.17.1): チャンクが文の途中から始まったとき Gemini が続きを創作していた不具合を修正。「この音声は断片であり、途中で始まり途中で終わるのが正常」とプロンプトで明示した
- 2026-08-31 (v0.17.2): 短チャンク統合の段で、文の途中で切れた断片どうしを繋ぎ直すようにした（両方の断片が手元にある場合だけ）
- 2026-08-31 (v0.17.3): 確定待ちの文字起こしを残したまま Notion に保存して閉じると内容が消える事故を防止
- 2026-08-31 (v0.17.4): 見出しのある段落で2つ目以降の本文がコピー・要約・質問から消えていた不具合を修正（getConfirmedText が最初の .p-body しか見ていなかった。同じバグは別の場所では修正済みだった）
- 2026-08-31 (v0.18.0): Gemini チャンクを12秒固定ではなく**無音の切れ目**で区切るように変更（最短12秒／最長20秒）。v0.17.1・v0.17.2 の根本原因を除去。切り口が無音か強制かを Gemini に正直に伝える。ノイズフロアはその場で実測し、静かなときだけ学習する
- 2026-08-31 (v0.18.1): 無音区切りの副作用で、末尾に「（音声不明瞭・再試行可）」が必ず出るようになったのを修正（最後の区切りのあとに沈黙だけのチャンクが必ずできるため）。Gemini・Web Speech・無音検出器の3つが揃って「何も無い」と言った場合だけ表示しない
- 2026-08-31 (v0.18.2): 無音のしきい値 350ms が短すぎて**言いよどみ**で切っていたのを修正（「しかし、最初冒頭／の方に喋り始めた」と真っ二つになった）。通常 700ms、最長の4秒前からは 350ms に妥協する段階方式へ。しきい値の判断を decideChunkCut に集約。診断ログにチャンク長を追加
- 2026-08-31 (v0.18.3): **無音区切りが最初から一度も機能していなかった**のを修正。開始直後のゼロ(-99dB)がノイズフロアの初期実測に入り、片方向学習のせいで回復不能になっていた。フロアを「直近の窓の最小値」方式に作り直し（最大値との開きが10dB未満なら判定を放棄）。診断ログに床/天井/最長無音/AudioContext状態を追加
- 2026-08-31 (v0.18.4): 実機のマイクが音量を平坦化していて（発話と沈黙の差が4dB）音量では判定できないことが判明。並走中の Web Speech を第二の判定手段にした（音声認識器の判断なのでゲインに影響されない）。優先順位は pickSilentMs に集約し、2つを混ぜない。マイクの実設定・チャンク全体の振れ幅・採用した手段をログに追加
- 2026-08-31 (v0.18.5): v0.18.4 の「外部で平坦化されている」という説明は誤りで、**測る場所が間違っていた**。全帯域の RMS はエアコンなどの低域に支配され、その上に乗る小声の差が埋もれる。人の声の帯域（300〜3400Hz）だけを見るように変更。同じ音で差が 0.3dB → 15dB になった。テストのモックも周波数ごとに音を置けるよう作り直し、空調＋小声／会場変化のケースを追加
- 2026-08-31 (v0.18.6): v0.18.5 の実機結果は良好（幅31〜36dB・強制区切りゼロ）。残っていた2件を修正: (1) 停止時の最終整形が最後の音声チャンクを待たず、その段落だけ繋ぎ直しから漏れていた → waitForAudioSettled を追加 (2) 950ms の言いよどみを文の切れ目と判定していた → 実測値（文末1350/1300ms・言いよどみ950ms）の間に線を引き 700→1000ms へ
- 2026-08-31 (v0.18.7): 123字の録音から**話していない議事録が丸ごと捏造された**のを修正。原因はプロンプトが「主要ポイントを5〜8個」と入力に対して過剰な量を要求していたこと（「創作はしない」と書いてあっても、具体的な指示のほうが強く働く。v0.17.1 と同じ構造の3回目）。数の要求を外し、短い入力が正常だと教える方向に変更
- 2026-08-31 (v0.18.8): Web Speech の確定が遅れて次のチャンクに付き、すでに Gemini が書いた最後の一文が二重に表示されるのを修正。時刻での対応づけは不可能なので、すでに画面にある内容と2文字の並びが70%以上重なるものは表示しない（同じ内容が残っていることを確認したうえで消すので取りこぼしにならない）
- 2026-08-31 (v0.18.9): 3.8秒の沈黙を検出していたのに「最短12秒より前だから」と捨て、20秒の強制区切りに落ちていたのを修正（末尾の「…コピーして送り」で続きが失われていた）。3秒以降なら2秒以上の無音で切れるようにした。あわせて、停止処理中にミドル整形が先に発火して最後の1段落が取り残される問題も修正
- 2026-08-31 (v0.18.10): v0.18.9 の実機結果は狙いどおり（4本すべて `頭=無音 尻=無音`・強制区切りゼロ・11.2秒で切れて最短前の切れ目も機能）。残っていた末尾の「（音声不明瞭・再試行可）」を修正: 発話判定が1フレーム(50ms)で成立していて物音を発話と誤認していたので 200ms 連続を要求。あわせて、停止時の3秒未満の末尾は両エンジンが無音なら消す
- 2026-08-31 (v0.18.11): 録音開始から3.0秒で、まだ一言も喋っていないのに区切って送信していたのを修正。v0.18.4 で Web Speech の活動時刻に開始時刻を入れていたため「開始からずっと無音」に見えていた。0 のままにして、実際に聞き取るまでこの手段を使わないようにした（レベル検出器の canJudge と同じ原則を Web Speech 側にも適用）
- 2026-08-31 (v0.19.0): 全文やり直しの**土台**。録音音声を IndexedDB に一時保管（audio-store.js）。**既定は保持しない**（録音の保存に対する同意は文字起こしより狭いことが普通にあるため）。保持する場合も、タブが消えたら必ず消す・起動時に期限切れを掃除・未知の設定値は消す側に倒す。ビットレート既定を 64kbps に。Files API がブラウザから使えることを CORS プリフライトで実測済み。やり直し本体は次の版
- 2026-08-31 (v0.19.1): 診断ログの不備2件を修正。(1) 保持のオン/オフがログに出ず、動作確認をログから判断できなかった → 録音開始時に出す (2)「最長無音」が前のチャンクから続く沈黙を合算していて、チャンク長より大きい値が出ていた → チャンク長で頭打ちにし、前から続く分は `+` で示す
- 2026-08-31 (v0.19.2): 実素材（ニュース8分）でのテスト結果を反映。(1) 空チャンクを消す判断に使う hadSpeech をログに出していなかったので追加（v0.19.1 と同じ反省の再発）(2) 重複検出が8文字未満を対象外にしていて「乗り越えるべき」(7文字) がすり抜けたので、完全一致なら4文字から判定するよう下限を2段階に分けた
- 2026-08-31 (v0.19.3): 残っていた「（音声不明瞭・再試行可）」の正体は**番組終わりのジングル（音楽）**だった。hadSpeech は「発話があったか」ではなく「大きい音があったか」を見ているだけで、鳴り続ける音楽は素通りする。音楽判別は費用対効果が合わないので作らず、代わりに文言の嘘（不明瞭ではない／再試行しても何も起きない）を直して、聞き取れなかった秒数を出すようにした
- 2026-08-31 (v0.20.0): **語彙辞書**。プリセット6件（教育・ビジネス・医療・IT・行政・研究）＋自作辞書。合成は 手入力 → 辞書 → 自動 の順（上限50語）で、辞書を選んでも自動辞書は消さず足す。フッター左に「いま何が効いているか」を常時表示（右の拡大ルーラーと対）。自動で育った語に名前を付けて自作辞書に昇格できる
- 2026-08-31 (v0.21.0): **話者判別**（当初からの本命）と、その前提として**保管の作り直し**。v0.19.0 が貯めていたチャンクは `stop()/start()` で作った独立した webm なので、繋いでも最初の12〜20秒しか読めなかった（Segment がサイズ不明で書かれるため、デコーダは2本目以降を1本目の中身として読む）。live とは別の MediaRecorder を `start(timeslice)` で回し、約10分ごとの「区間」に分けて保管する形に変更。アップロードは **multipart**（レジューム可能は応答ヘッダ `X-Goog-Upload-URL` を読む必要があるが、実測した `Access-Control-Expose-Headers` に無いので依存しない）。区間をまたいだ話者の対応づけは原理的に保証できないので、完了時に画面へ明記する
- 2026-08-31 (v0.21.1): 話者判別のあと音声が消えたことを、**消した瞬間に**言うようにした。v0.21.0 は黙って消し、次に押したとき「保管された録音がありません」とだけ出していた。消えるのは設計どおりだが、心当たりの無い理由しか挙げていなかったので設定を直しようがなかった。あわせて `persistSessions()` の容量超過を黙らなくした
- 2026-08-31 (v0.21.2): 「間違えて元に戻すボタンを押したら、もう2度と話者判別はできないのでは」という指摘への対処。**消えて困るのは録音ではなく結果そのもの**なのに、いちばん高価で二度と作れないものをリロードで消える取り消し履歴にしか置いていなかった。結果を `session.repass` に残し、同じボタンから貼り直せるようにした（貼り直しも `pushUndo` してから行うので2つの版を行き来できる）
- 2026-08-31 (v0.21.3): **表現の修正と既定の変更**。「やり直し」は "失敗をもう一度やる" の意味で、話者判別はやり直しではない（やっさん指摘）。画面・ログ・コメントの表記を**話者判別**に統一し、関数名も `runDiarization` などに揃えた。**保存されるキー（`session.repass` / `audioRepassDoneAt` / 設定値 `'repass'`）は据え置き**（変えるとすでに保存された結果と設定が読めなくなる）。あわせて保持の既定を `repass` → `close` に変更。どちらもタブを閉じれば必ず消えるので「できるだけ早く消す」方針は満たしたまま、**同じ録音に2回目をかけられる**
